# 🧩 기능 카탈로그 (개요)

알뜰이 제공하는 기능의 **개요·인덱스**입니다. 앱은 **두 모드**로 나뉘며, 상세는 모드별 문서를 보세요. 데이터 구조는 [data-model.md](data-model.md), 함수 위치는 [code-structure.md](code-structure.md) 참고.

| 모드 | 무엇 | 상세 문서 |
|---|---|---|
| 💰 **가계부** | 개인·그룹 공유 가계부(거래·자산·카드실적·예산·구독·목적별·정산·경조사비·대출) | [features-ledger.md](features-ledger.md) |
| ✅ **할일** | 개인/그룹 공유 할일(프로필별 관리·친구 공유·담당 배정·반복·여행 연동) | [features-todo.md](features-todo.md) |

## 모드 전환 / 화면 구성

- **상단바**: **모드 토글**(`[가계부 | 할일]`, `setMode` — `localStorage('mode')` 유지), **워크스페이스 칩**(현재 가계부 전환), **펫캠 토글**, **테마 토글**(🌙/☀️). 상단바 아래엔 **알뜰홈 dock**(걸어다니는 도트 고양이 + 은화)이 상시 노출됩니다. **펫캠을 끄면(근무 모드)** 좌상단 픽셀 앱 아이콘 대신 **텍스트 "알뜰"** 이 표시되고 아이콘 위 알림 표시(빨간 점)도 함께 숨겨집니다(`body.camoff`, `renderDock`가 토글 — 탭 동작은 동일). **좌상단 아이콘 알림은 빨간 점 하나(아이콘 우측 상단, 숫자 없음)** — 안 받은 선물·안 쓴 쿠폰·안 본 공지(새 업데이트) 중 하나라도 있으면 표시(`updateNewsBadge`/`newsUnread`, 소식 진입 시 공지분은 읽음 처리 — 과거의 숫자 뱃지·미처리 초록 점은 이 점 하나로 통합).
- **하단 탭바**는 모드별로 바뀝니다(`renderTabBar` / `_TABSETS`). 가운데 **＋** FAB는 모드에 따라 거래 추가/할일 추가로 분기(`fabAdd`). **더보기**는 두 모드 공용 진입점이지만 모드에 맞는 메뉴를 보여줍니다(`renderMore` 분기).
- **오늘 홈(랜딩)**: 상단 **로고=홈 버튼**(`goHome`)으로 진입하는 오늘 대시보드(오늘 미션·할일·지출 한 줄). 오늘 미처리는 **할일 탭 점**(`applyTodoTabDot`)으로만 알리고(로고 점은 소식 빨간 점 하나로 통합되며 제거), 다 처리하면 **완료 카드**(고양이 + 1회 축하 연출, `prefers-reduced-motion` 정적)로 끝맺음을 줍니다. 남은 일 계산은 단일 순수 헬퍼 `todayPending`(util.js).

| 모드 | 하단 탭 |
|---|---|
| 가계부 | 자산 · 캘린더 · ＋(거래) · 리포트 · 더보기 |
| 할일 | 할일 · 캘린더 · ＋(할일) · 완료 · 더보기 |

## 💰 가계부 모드 (요약)

개인·그룹이 함께 쓰는 공유 가계부. **거래 9종**(수입/지출/이체/충전/선불결제/환불/포인트±/잔액조정)을 기록하고, 달력·리포트로 보고, 자산·카드실적·예산·정기결제·구독·목적별 가계부·정산·경조사비·대출·적금을 관리합니다. **소비 대상(개인/공동) 분리 집계**, **해외통화(여행) 실시간 환율**, **CSV 내보내기** 지원.

→ 전체 내용은 **[features-ledger.md](features-ledger.md)**.

## ✅ 할일 모드 (요약)

개인이 **프로필별로 할일**(user-global — 워크스페이스와 무관하게 항상 동일, `users/{uid}/todos`)을 관리하고, **친구 코드로 친구를 맺어** '할일 공개'를 켠 친구의 할일을 읽기전용으로 봅니다. **그룹 할일**로 여행·모임의 담당을 나눕니다. **스코프는 그룹전환(컨텍스트)이 결정** — **개인 프로필=내 할일, 그룹=그룹 할일**(개인/그룹 세그먼트 폐지, 개인용은 그룹전환에서 '개인 프로필' 선택). 개인 프로필일 때만 상단 **[내 할일 | 친구들] 토글**로 **친구들 피드**(공개 친구 아바타·오늘 무지개 테두리·친구 할일 목록, 친구 관리는 더보기 공용)를 봅니다. 마감 캘린더·반복·목적별(여행) 연결·완료 시 **은화 보상** 제공. 할일마다 **카테고리(색)**를 붙여 목록·캘린더를 색 점으로 구분하고, 캘린더 점은 **미완료=마감일·완료=완료한 날** 기준으로 표기(상세 [features-todo.md](features-todo.md)).

→ 전체 내용은 **[features-todo.md](features-todo.md)**.

## 🐱 알뜰홈 (은화 경제 — 게임화)

가계부·할일 활동으로 **은화**를 모아 **고양이**를 입양·구경하는 게임화 기능. 소속은 **개인 전역**(`users/{uid}/game`)이라 개인↔그룹을 오가도 내 고양이/은화가 따라옵니다. 자세한 기획은 [cat-feature-plan.md](cat-feature-plan.md).

- **은화 미션**: 일일 미션 — **고정 2종**(오늘 1건 기록 +50 / 출석 +50)에 더해 **날짜 시드 로테이션 2종**(`DAILY_POOL`: 카테고리+메모 채우기·거래 3건·할일 완료·펫 쓰다듬기 중 매일 2개, 각 +40)을 KST 자정 기준으로 판정, **멱등 수령**(같은 날 중복 지급 불가). 출석은 진입 시 자동 수령. 접속만 해도 받는 '기본 번들'을 크게 해 라이트·헤비 격차를 좁힘(경제 정책 [economy-policy.md](economy-policy.md)). **업적**(1회성): 첫 거래·고양이 3마리·여행 가계부·해외통화 첫 지출·첫 예산·첫 정산·**첫 할일 완료·할일 10개 완료** 등 앱 기능을 써보면 은화 지급(`ACHIEVEMENTS`, period `once`로 영구 저장). **내 미션(커스텀 습관)**: 미션 화면 '내 미션' 섹션에서 직접 추가(`openCustomMissionEdit`), **매일 체크는 무보상**·**7일 연속마다 +40 은화**(최대 5개, 개인 전역 `game/customMissions`·`game/missionLogs`, 날짜키 멱등·재지급 없음, 주간 리셋 없이 영구 지속), 🔥연속·최근7일 점 표시. 업적에 '첫 내 미션'·'7일 연속'도 포함. **매일 반복 습관은 내 미션**, **정해진 날짜 반복(매주·매월)은 할일 → 반복**으로 역할 분리(겹침 제거).
- **도트(픽셀) 아트**: 고양이(고등어·치즈·삼색 코숏)·은화·가구를 문자 매트릭스→SVG로 렌더(다크모드 자동 대응). **동물 <!--@gen:cats-full-->148종 전부(고등어·뒤뚱·길냥·네로·하양·복슬이·검정얼룩이·골목대장·삼삼이·황토·폴드·보라·초코·아깽이·핑크·고랑이·갈기냥·페르시안(흰색)·백호·블루·얼룩이·시고르자브·블랙팬서·백구·시바·코기·달마시안·닥스훈트·불독·인절미·스탠다드푸들·비글·숙희·도베르만·퍼그·저먼셰퍼드·보더콜리·스피츠·잭러셀테리어·레브라도·차우차우·카디건코기·그레이하운드·시츄·세인트버나드·보스턴테리어·바셋하운드·해피·웰시테리어·파피용·뉴펀들랜드·비어디드콜리·보더콜리·로트와일러·포인터·파라오하운드·웨스트하이랜더테리어·바이마라너·콜리·잉글리시불독·키스혼드·프렌치불독·요크셔테리어·토이푸들·셰틀랜드십독·미니어처핀셔·슈나우저·골든두들·버니즈마운틴독·캐벌리어스파니엘·아키타·휘핏·올드잉글리시쉽독·비즐라·잉글리시셰터·진돗개·차이니즈크레스티드·스코티시테리어·포메라니안·샤페이·그레이트데인·불테리어·복서·로디지안리지백·아이리시세터·에어데일테일러·사모예드·시베리안허스키·고등어·칼리코·하양·치즈·턱시도·샴·벵갈·러시안블루·스코티시폴드·까망·설렁·페르시안(회색)·메인쿤(갈색태비)·아메리칸숏헤어(실버태비)·랙돌(포인트)·터키시앙고라(흰색)·먼치킨(삼색)·노르웨이숲(갈색)·봄베이(검정)·아비시니안(갈색)·스핑크스(핑크)·브리티시숏헤어(그레이)·벵갈(스노우)·장모 삼색·토터셸(카오스)·샴(초콜릿포인트)·코니시렉스·오시캣·셀커크렉스·코랫·맹크스·아메리칸컬·데본렉스·터키시반(반무늬)·밥테일·버미즈·히말라얀·크림태비·라일락·소말리·삵·시라소니·치타·재규어·퓨마·눈표범·카라칼·표범·흑표범·오셀롯·모래고양이·메인쿤(블랙스모크)·메인쿤(레드태비)·벵갈(실버)·피더볼드·토이거·싱가푸라·하바나브라운·라가머핀·구미호)<!--@gen:end--> PixelLab PNG 스프라이트 시트(288×48 6프레임)로 CSS `steps()` 걷기**(`PET_SPRITES`/`catActorHTML`, 시트 없는 동물은 SVG 매트릭스로 폴백). **옆으로 이동할 땐 옆 걷기 시트만**(east 기준, 서쪽은 `scaleX(-1)`), **가만히 서 있거나 캣타워·방석 등에서 쉴 땐 정면(south) 정지 이미지**로 전환(정면으로 서 있는 시간을 길게). 걷기 애니메이션 주기는 이동 속도에 맞춰(`--wdur`/`walkDur`) 발이 바닥을 미끄러지지 않게(무빙워크 방지). 옆(east) 걷기 시트가 없는 export는 `frontWalk`로 **이동 중 east 정지스틸만 보여 정면 걷기를 방지**(현재 14종 전부 east 옆걷기 보유 — 해당 없음). 새 동물 zip 처리 규칙은 [pet-asset-pipeline.md](pet-asset-pipeline.md).
- **펫별 크기 배율(`scale`)**: 종별 몸집을 반영(고양이=1.0 기준, 예 호랑이 5·곰 4·강아지 1.5·토끼 0.8·거북이 0.9·새 0.6). `PET_SPRITES[id].scale`(`tools/pets.json`의 `scale`에서 생성)을 `petScale()`이 읽어, **걷는 무대(웹캠 dock 방·알뜰홈 홈 방)에서 렌더 높이=기준×scale**로 반영(무대 밖으로 안 나가게 상한 클램프 — dock 96px·홈 200px). 알뜰샵/보유칩/뽑기 썸네일은 초상 프레임이라 균일.
- **전역 dock**: 모든 화면 상단에 얇은 스트립으로 은화 잔액 + 걸어다니는 고양이. `prefers-reduced-motion`·백그라운드 시 애니메이션 정지. **캠/방에서 펫을 바로 끌어(드래그) 좌우로 이동**할 수 있음(꾹 누를 필요 없이 살짝만 끌면 집힘, 놓으면 그 자리에서 다시 배회, `petGrabDown`). 드래그 중 이미지/텍스트 선택은 방지(`user-select:none`+preventDefault). **dock LIVE 배지를 탭하면 알뜰홈**이 열리며(누르는 press 애니메이션 후 열림, `coinTap`→`openCatHouse`), 캠 빈 곳 탭은 아무 동작도 하지 않음. 캠 **우상단엔 [행복도❤]+[수확]+[지갑 카운터]**(은화·금화, `walletHtml`/`.cd-wallet`) — **지갑은 평소엔 숨겨져 있고(트랜지언트, LIVE 배지 가림 방지) 재화 획득 순간에만 ~2.6초 팝인**(`walletShow`/`walletVisible`, 재렌더에도 만료시각 기반으로 표시 유지): 쓰다듬기·수확·똥치우기 보상이 지갑으로 날아와 카운트업 후 사라진다(`rewardFly`). 수확 시엔 버튼 근처에 **획득량 "+N 은화(+금화)" 픽셀 플로팅**(`yieldFloatFx`)이 함께 떠오르고, **은화만 얻은 평범한 수확은 토스트 생략**(금화·드롭·밥물 채움·부족 안내가 있을 때만 토스트). **[수확]**(구 돌보기)은 **모든 방**의 **유휴 가구수익**을 받고 밥·물·똥까지 **한 번에**(방이 여러 개여도 1탭·현재 방부터 채워 소모품 부족 시 보이는 방 우선) 정리한다(`allRoomsIdleYield`/`batchCare`). **소식 화면**(알림·이벤트·공지·쿠폰)은 **좌상단 브랜드 아이콘**(`openNews`)으로 진입. 게임 허브(알뜰홈)는 더보기 → 알뜰홈으로도 진입. **🖥️ 펫캠 PiP 미니 창(데스크톱)**: 캠 우하단 픽셀 PiP 버튼(`.cd-pip`)으로 현재 방 캠을 **항상 위(always-on-top) 미니 창**으로 띄워 다른 창에서 작업하면서 볼 수 있음(시청 전용·백그라운드에서도 계속 움직임). **방식은 설정에서 선택**(더보기 → 설정 → "펫캠 PiP 방식", `openPipModeSheet` — 지원 브라우저에서만 행 노출, 버튼 탭=열기/닫기만): 기본 **🎬 비디오 PiP**(canvas→워커 push→video PiP) = 유튜브처럼 **평소엔 방 화면만, 호버 시에만 컨트롤**이 보여 깔끔 — 씬·펫·**가구 연출(FURN_ANIM 전부)·배경효과·움직이는 벽지/바닥 씬**을 Web Worker(OffscreenCanvas)가 드로잉하고 **프레임을 push 방식(`MediaStreamTrackGenerator`)으로 직접 공급**해 탭 숨김·앱 백그라운드(모바일)에도 계속 움직임(캡처스트림 pull 방식의 백그라운드 정지 문제 해결, 미지원 브라우저는 captureStream 폴백) — dock와 동일하게 전부 살아 움직임(keyframe·속도·회전중심을 JS로 전사, `_VPIP_FX_*`·파티클). **🪟 창(Document PiP)** 은 설정에서 선택하거나 비디오 PiP 불가 브라우저(Firefox 등)에서 자동 폴백 — DOM 완전 미러(엔진 무대 편입), 상단바(주소·X)는 브라우저 보안 UI라 표시(`disallowReturnToOpener`로 "탭으로 돌아가기"만 숨김), LIVE 배지·방 이름은 창 안에 안 그림(방 이름=창 제목). 방·벽지·가구·펫·테마 변경은 두 방식 모두 실시간 반영, 버튼 재탭 또는 창 닫기로 종료, 미지원 방식은 반대쪽 폴백. **안드로이드(Chrome·TWA)는 🎬 비디오 PiP 사용 가능**(창 방식은 데스크톱 전용 — 길게 눌러 전환 시 안내만), **아이폰/아이패드는 버튼이 아예 표시되지 않음**(`_pipIOSLike` 게이트 — `canvas.captureStream` 미지원이라 원천 불가).
- **🔋 적응형 성능(모바일 우선)**: 기기 등급을 부팅에 자동 감지(`perfTier`)해 캠 부하를 조절한다 — **자동 등급은 `high`/`mid`뿐, 절대 `low`(=body.lite 전면 정지)를 내지 않는다**(2026-07-10 사용자 지침: 아이폰 사파리가 `hardwareConcurrency`를 낮게 보고해 정상 아이폰이 low 오판 → 저사양 OFF인데 연출 전면 정지 장애). **데스크톱 = 항상 `high`(무감축)**, **모바일 = `mid` 고정**(`body.perfmid` — GPU 레이어 회수·씬 데코 개수 ×0.7 `perfCountMul`·걷기 프레임예산, **움직임은 전부 유지**). `body.lite`는 오직 ①사용자 수동 '가벼운 모드' 토글 ②`perfTier` 수동 오버라이드로만. 저사양 추정 기기(코어4↓/메모리4GB↓)엔 **가벼운 모드 1회 권유**(`maybeSuggestLite`, opt-in)만 뜬다. `deviceMemory`·`hardwareConcurrency`는 저보고 오판(데스크톱·아이폰 2회 재발)으로 low 판정에 쓰지 않는다. 사용자 수동 '가벼운 모드'(설정)와 별개로 겹쳐 적용되고(자동은 제약을 더할 뿐 사용자가 켠 lite를 끄지 않음), 실행 프레임이 지속적으로 밀리면(과부하) fps를 한 단계 더 낮췄다 회복하는 **적응형 프레임예산**(`_autoBudgetMul`, `catLoop`)도 돈다. 원리상 연속 CSS 애니는 '느리게'가 아니라 **개수 감축·정지·GPU 레이어 회수**로만 발열이 줄고(매 리프레시 재합성), `steps()` 걷기 필름만 duration↑로 리페인트가 실제로 줄어 그쪽만 느리게 한다. `localStorage 'perfTier'='low|mid|high'`로 강제 지정 가능(QA). PiP는 데스크톱 전용이라 모바일 경로엔 개입하지 않는다.
- **알뜰홈 시트**(더보기 → 알뜰홈, 아이콘=은화): **홈**(방 LIVE 뷰 + 걷는 고양이 + 배치 가구 + 돌봄(밥·물·똥·수확) — **이 방 활성 슬롯 기본 3 + 잠금 슬롯 금화 100 확장**, 펫 관리 그리드는 별도 **펫** 탭으로 분리해 홈 과부하 해소) / **펫**(`catPetHtml` — 우리집 펫 컬렉션 관리: 종류(species) 탭 + 정렬(`_petSort`), **배치모드 토글(기본 OFF)** — OFF: 탭=펫 정보(애정도·코스메틱·이름변경, 오탭 배치 방지) / ON: 탭=이 방으로 데려오기·대기(`_petPlaceMode`/`petTileTap`). 타일의 ✎이름·ⓘ정보 아이콘은 제거(이름변경=펫 정보 시트). 그리드 노드는 `renderCatHouse`가 보존 재사용) / (**알뜰샵**은 `openShop`으로 분리 — 고양이·가구·**소비**(사료·물)·벽지·가챠 탭, 가구 수량 허용 — **가구 탭엔 전체·케어·휴식·놀이·장식 분류 필터**(`_shopFurnCat`, 배치 인벤토리와 같은 `ITEM_CATALOG.cat` 기준), **펫 탭엔 종(species)별 분류 탭**(전체·고양이·강아지·… `_shopPetSpecies`/`setShopPetSpecies`, 카탈로그에 실제 있는 종만·2종↑일 때) **+ 정렬 셀렉트**(등급↑기본·등급↓·이름·**미보유 먼저**, `_petShopSort`/`sortShopCats`, 선택 유지) — **고양이 카드를 탭하면 선택**되어 강조되고 썸네일이 옆으로 걷는 스프라이트로 미리보기됨 — 이름 오른쪽 분류 태그는 **품종이 아니라 종(species) 라벨**(고양이/강아지/토끼, `SPECIES_LABEL`)) / **배치**(가로12×깊이8 격자 — 짧은 바닥에 뒤쪽이 구분되게 깊이 12→8행 축소, 캣타워=1×2·스크래처=1×1·배변패드=1×1·화분=1×1·펫하우스=1×1(그래픽은 크게)·캣휠·러그=2×2칸 점유·겹침 방지 — **러그는 바닥 아이템(`floor`)이라 겹침 허용·맨 뒤에 깔려 위에 가구를 올릴 수 있음**, **꾹 눌러(롱프레스) 드래그로 배치·이동**(화면 스크롤과 겹치지 않도록) — 짧게 탭하면 팔레트=선택·배치된 가구=회수/판매 메뉴. **가구 팔레트는 보유중인 가구(itemQty>0)만 케어·휴식·놀이·장식 카테고리 탭으로 묶어 그리드로 보여줌**(펫 인벤토리와 동일한 방식·정렬/검색 없음, 분류 단일 소스=`ITEM_CATALOG.cat`·`placeCatOf`·`PLACE_CATS`·`.palette.catinv`). 전부 배치돼 남은 수량이 없는 가구는 **흐리게+"전부 배치됨"**(`.pitem.soldout`), 보유가 없는 카테고리 탭은 흐리게, 빈 분류엔 **알뜰샵 가기 CTA**(`.palcta`). **[방꾸미기 | 벽꾸미기] 토글**로 바닥/벽을 나눠 꾸밈 — **벽꾸미기=벽 12×4 격자**(위=천장·아래=바닥선, 칸 구분선 표시)에 **벽 가구**(`wall:true`: 벽난로·창문·벽시계·행잉플랜트·모빌 — 모두 1×1)를 걸고(바닥엔 못 놓음) — **방꾸미기와 동일하게 팔레트를 꾹 눌러 격자로 끌어 배치하거나 걸린 항목을 드래그로 이동**(탭 배치도 유지, 짧게 탭=선택/회수·판매), 캠 3무대(dock·홈·친구)의 벽면(위 46%)에 표시·연출. **방꾸미기엔 바닥 선택·벽꾸미기엔 벽지 선택 스와치 줄**(`skinPickerHtml`)이 있어 보유한 바닥/벽지를 탭해 현재 방에 바로 적용(상점 안 가도 됨)). 시트 탭은 **홈·펫·배치**(`catseg`) — **룸 스위처(`roomStripHtml`)는 헤더 바로 아래에서 1회만 렌더해 세 탭이 공유**(펫 탭에서도 대상 방 확인). **알뜰샵**은 별도 진입(`openShop`, 제목 스왑) — 알뜰샵의 은화 매수탭(고양이·가구·벽지·바닥)은 현재 휴면(`SHOP_LEGACY_BUYTABS=false`, `SHOP_SUBS`=이벤트·펫알·랜덤박스·무지개·소비). 배치 팔레트 항목엔 `보유 M · 남은 N` 수량 표기. **미션**은 더보기 → **미션**(`openMissions`, 일일+내미션+주간+월간+업적+스트릭)으로, **소식**은 알뜰 아이콘 전용 화면(`openNews`)으로 분리했다.
- **기구물 리스트·배치 폴리시**: 상점 가구 카드·배치 팔레트에 **등급 링(`.tbring`)+등급명 배지(`.ptier`)**(펫과 공통), 상점 가구 **정렬**(등급↓/등급↑/이름, `_furnSort` — 펫 탭도 `_petShopSort`로 동일 패턴), 배치된 가구 탭 메뉴에 **등급·설명·크기** 표시. **특별(epic)↑ 등급 가구의 판매는 확인 시트를 한 번 거침**(판매가가 정액 10은화라 전설·신화 가구 실수 판매 방지, `sellPlaced`→`confirmSheet` — 회수는 그대로 즉시). 배치 화면엔 **자동 정리(`autoArrangeRoom`)·되돌리기(1스텝, `captureUndo`/`undoPlace`)·이 방 비우기** 버튼과 **빈 격자 첫 사용 안내**. **자동 정리**는 현재 방 배치를 **뒤→앞·발자국 큰 것부터·등급순**으로 격자에 다시 채운다(큰 쇼피스=뒷줄, **케어 그릇(밥·물·화장실)은 앞줄에 나란히 묶어**·작은 소품=그 뒤 앞줄, 바닥아이템=z:0 베이스, 벽 가구=앵커별 선호 행). 마지막에 **배치 전체 바운딩박스를 가로 중앙으로 균일 이동**(왼쪽 쏠림 해소 — 균일 시프트라 상대 위치·겹침 그대로). 기존 헬퍼만 재사용(`areaFree`/`occupiedCells`/`isFloorItem`/`CARE_ITEMS`/`wallAreaFree`)·단일 `roomTx` 커밋·`captureUndo`로 한 번에 되돌리기·확인 시트·`filledAt`+`fillMs`(그릇 채움·지속) 보존, **펫은 자율이라 미변경**. 확인 전 방 id를 고정해 대기 중 방이 바뀌면 취소(멀티기기 클로버 방지), 정리 후 배치 탭을 다시 열어 결과·되돌리기를 노출. 개발자 기구물 관리도 **등급별 섹션**. 탭·정렬 선택은 localStorage로 유지.
- **구조물·벽지·바닥 카탈로그**: 구조물 99종(케어·휴식·놀이·장식, 벽 가구 24·바닥 아이템 5 포함)·벽지 21종·바닥 19종. 전부 인라인 픽셀아트(도트 매트릭스, `ITEM_CATALOG`/`WALLPAPER_CATALOG`/`FLOOR_CATALOG`). 일부 구조물은 펫이 다가가 상호작용(`INTERACTIVE_FURN`/`furnSpot`), 다수는 캠(dock·홈 LIVE)에서만 움직이는 연출(`FURN_ANIM`, 회전·흔들림·깜빡임·일렁임). 벽지·바닥은 상점용(일반 등급·은화 구매) + 랜덤박스용(특별↑·가챠 전용)이 섞여 있다.
- **🎞️ 펫 다중 모션 클립**(PixelLab 프리셋, 순차 취득): 걷기 외에 **유휴(idle)·앉기(sit)·배깔기(belly)·먹기(eat)·마시기(drink)·하품(yawn)·하악질(angry)** 클립(적용 7종)을 펫별로 추가할 수 있다(`PET_CLIPS` 레지스트리 + 폴백 체인 — 클립 없는 펫은 기존 스틸/걷기 그대로). 자동 연결: **채워진** 밥그릇=먹기·물그릇/자동급수기=마시기(빈 그릇=앉기), 앉는 가구=앉기(1회 전환 후 유지), 눕는 가구=배깔기, 캣휠=달리기, **액티브 10종(2026-07): 트레드밀·레이저룸바·RC쥐·슬라럼·스프린트트랙=제자리 질주, 오이=하악질, 우유바=마시기·간식디스펜서=먹기, 버드피더·햄스터케이지=관람(eyetrack)** — 기존 레이저포스트·쥐돌이·깃털·볼트랙도 확률 질주, 유휴 중 가끔 하품(드물게 하악질) 원샷, 잠들기 전 하품. (run·jump·sleep·lick 클립은 폐기·보류 — 엔진 키는 남아 폴백 재생: run=걷기 필름 고속·jump=생략·sleep=north 스틸.) dev 펫 zip에 프리셋 애니 폴더가 있으면 자동 인식(런타임 `catalogPets/{id}.clips`+`catalogPetArt/{id}.clips`, 정적은 `<클립키>.png`+`pets.json clips`). 모션축소·가벼운 모드에선 기존 정지 스틸(절전). 취득 우선순위·규격은 [pet-asset-pipeline.md](pet-asset-pipeline.md) "다중 모션 클립" 절. **신화·한정 전 펫(표범+15종=16종)이 적용 7종 클립 보유**(2026-07 `tools/pet_motion_build_all.py` 일괄 제작 — 종별 차별화: 대형=하품 송곳니, 사자=으르렁+갈기 호흡, 카라칼·시라소니=귀 눕혀도 술 유지, 진돗개=귀 직립+이빨 드러냄, 옆꼬리 펫=idle 꼬리 스윙·재규어/치타=머리 위 꼬리 끄덕·그 외=귀끝 트윗치).
- **활성 슬롯 확장**: 집에 내보낼 수 있는 슬롯이 기본 3칸. 홈 상단 슬롯 행 오른쪽의 **잠금 슬롯(🔒 금화 100 표기)** 을 탭하면 **금화 100**으로 4번째 슬롯을 영구 확장(`home.slots` 3→4, `buySlot`/`slotCount`, 금화의 첫 소비처, 최대 20). dock·방 렌더와 내보내기 상한이 모두 **현재 방의** 슬롯 수를 따름.
- **여러 방(프리셋)**: 알뜰홈을 **여러 개의 방**으로 관리(게임 프리셋 방식). 방마다 **가구 배치·벽지·펫이 독립 저장**되고, 홈/배치 탭 상단의 **미니 미리보기 썸네일**(벽지+가구 축소·펫수·이름)을 탭해 전환한다. 기본 1개, 오른쪽 **잠금 방(🔒 금화 500)** 을 탭하면 **금화 500**으로 확장(**최대 5개**, `home.rooms[]`·`roomSlots`·`current`, `buyRoom`/`roomCount`/`switchRoom`). 썸네일 ✎는 **방 관리 시트**(`openRoomMenu`): 이름 변경 + 다른 방 **벽지 가져오기**(`copyRoomWall`) + **방 비우기**(`clearRoom` — 가구는 인벤토리로 돌아가 다른 방에 재배치, 펫은 대기). 캠(dock)·홈은 **현재 선택된 방**을 보여주고(방 여러 개면 dock에 방 이름), **친구·랭킹 방문은 지정한 대표 방(`showRoom`)만** 노출(사적인 방은 **DB 레벨로 비공개** — 친구는 공개 스냅샷 `homeCam/{uid}`(대표 방만)를 읽고, `users/{uid}/game`(모든 방)은 규칙상 소유자만 읽음). 대표 방은 방 관리에서 ★ 지정·썸네일 ★ 배지. 방마다 **이모지**(`setRoomEmoji`) 지정 가능. **한 펫은 한 방에만** 있고(다른 방에서 탭하면 그 방으로 이동, `toggleActiveCat` 트랜잭션), 펫 카드에 **현재 방 배지**·상태(이 방/○○방/대기)가 표기돼 방 전환 시 즉시 바뀜. 방 **순서 변경**(썸네일 롱프레스 드래그 `moveRoomTo`, 키보드용 ← → 버튼도 제공). 가구 인벤토리는 전역이라 **모든 방 배치 합**으로 소진(복제 불가, `sumPlacedItem`/`itemPlacedAll`); 밥·물그릇 정산은 **안 보는 방도** 3시간 뒤 처리. (레거시 단일 방 데이터는 첫 로드 시 방 1로 자동 이관 — `util.js normalizeHome`.)
- **주간 미션**: **기록 계단 3/5/7일**(+60·+80·+120 은화, 7일은 +금화1)·리포트 확인(+40·+1 금화), KST 월요일 초기화·멱등.
- **월간 챌린지**(`MONTHLY_MISSIONS`, period `month`, `kstMonthKey`='M'+YYYY-MM): 이번 달 **15일 기록**(+300 은화·+5 금화)·**거래 25건**(+150·+3 금화), KST 매월 1일 초기화·멱등. 금화의 큰 공급원(꾸준함 보상).
- **금화 조직적 획득·희소성 방어**: 금화는 **주간·월간 미션·업적·로그인 스트릭·펫 애정 레벨업(만렙)·받은 좋아요·도감 마일스톤·유휴 수확 시간당 롤(10%)**에서 지급. **가챠 부산물 금화는 하루 2뽑까지만**(`grantGachaGold`, 은화→금화 세탁 차단) → 은화가 넉넉해도 금화는 희소 유지. 방(500)·슬롯(100) 확장이 무과금에도 도달 가능.
- **🔮 가챠 천장(pity)**: **종류마다 독립 카운터**(펫알·랜덤박스·뜰알·무지개알·무지개박스) — 그 종류를 **100뽑 안에 신화 이상 확정**(뜰알 확정은 **신화 50%·한정 50%**, 그 외 신화). 그 전에 신화↑를 뽑으면 그 종류만 리셋. **각 가챠 카드에 "신화확정 N뽑" 칩**(`game.pity`, `util.pityForced`/`pityNext`/`pityRemain`·`pickTierMember`). **알·박스는 가방에서 못 열고 가챠샵에서** 연다(가방=[가챠샵에서 열기], 가챠 탭에 보유 알/박스 열기 섹션).
- **⏭️ 10연차 리빌 SKIP·잠금**: 결과 카드 우측 상단 **SKIP**은 신화·한정·**처음 얻는(NEW)** 카드에서 멈추고(`tenSkip`), 같은 기준의 카드가 뜨면 **약 1초간 탭 잠금**(`tenShowCard`+`TEN_CARD_LOCK`, 잠금 중 '탭하여 다음' 힌트·SKIP 숨김/비활성 `.ten-lock`)으로 연타해도 지나치지 않게 강제로 보여준다.
- **🎁 가챠 일일 무료 1뽑**: 뜰알(한정 픽업)·펫알·랜덤박스 배너에서 **종류별 하루 1회 1뽑 무료**(은화·금화·보유 알/박스 전부 무소모, 펫알/랜덤박스의 금화 부산물(+1)도 없음 — 금화 희소성 방어). 무료 뽑기도 천장(pity)엔 똑같이 누적. **밤 12시(KST 자정) 자동 초기화**(`game.freePull` 날짜 마커 = `kstDayKey`, 트랜잭션 재검증으로 다기기 이중 사용 방지, `freePullAvail`/`bannerPull`). 남아 있으면 배너 1뽑 버튼이 **'오늘 무료!'** 로 강조되고 안내줄에 사용 여부가 표시된다(무료 소진 후엔 보유분 우선 → 재화 구매 순).
- **🌾 행복도 기반 자동 은화 + 다마고치 수확**: 방 펫이 **시간에 따라 자동으로 은화를 벌고**(방별 `harvestAt`, `util.roomYield`), **얼마나 버는지는 방 행복도가 좌우**한다(행복할수록 시간당↑ — happyFactor 0.2~1.0). **최대 24시간치까지** 누적(`roomYieldCapH`=24h — 하루 한 번 들러 수확하는 리듬), **[수확]** 버튼으로 받는다.
  - **행복도(❤%)** `util.roomMood` = **베이스 20 + enrichment(가구 '종류' 2종에서 포화·같은 종류 도배는 무의미) + 밥·물 신선도(28) + 평균 애정(16) + 수확 신선도(12) − 똥**. enrichment 대상은 **펫이 실제로 다가가 상호작용하는 가구 화이트리스트**(`INTERACTIVE_FURN` — `furnSpot` 케이스와 단일 소스, 도달 불가한 벽 가구(창문·벽난로)는 제외). 즉 **가구를 많이 놓는다고 오르지 않고**, 돌봄(밥·물)·사랑(애정)·수확을 챙겨야 100 근접(저사양·감성 꾸미기 유저 불이익 해소).
  - **전역 수익배율(1.0~2.0배, `util.yieldMultiplier`)** 이 자동수익에 곱해짐 = **펫 애정 총량(`totalAffectionLv`) + 도감 수집률(`dexProgress`) + 실제 앱 사용**. 특히 **오늘 가계부·할일을 기록하면 그날 수확 +부스트**(🍀 `recordedToday`)와 **주간 기록 습관**(`recordDaysThisWeek`) — 실제 앱 사용을 게임 수익과 직접 연결(캠 상단 수확칩 title에 `수익 x{배율}`·부스트 표시). 배율은 매 수확 파생·읽기전용(멱등, `_yieldMult`).
  - 수확 한 번이면 **밥·물 리필 + 똥 청소 + 수확 신선도 갱신 + 전 방 자동수익 수령**을 한꺼번에. 리필에 쓸 사료·물은 **소비 탭에서 기본↔고급으로 선택**(아래 소비템).
  - **🎁 실시간 드랍 스폰 + 클릭 수집**(`HARVEST_ROLL`/`reconcileDrops`): **1분 단위 확률 롤**(2026-07 — 롤 주기만 촘촘, 시간당 기대값은 동일: 금화 20%·랜덤박스 30%·펫알 16%·뜰알 4% + 🌈**무지개동전 1%·무지개알 0.2%·무지개박스 0.2%** × **방 행복도 보너스 배수** `dropMoodFactor`(10% 단위 내림 — 행복도 100=+100%·×2.0, 66=+60%·×1.6, 10 미만=기본·×1.0) × **💊 영양제 부스트**(`activeBoostMult` — 활성 시 ×1.5, 은화 수확과 동일), 24h캡)로 당첨되면 **한 방에만**(`dropTargetRoom` — 현재 방 우선, 방 수와 무관·**최대 5개 대기**) 바닥에 드랍이 놓여 반짝인다(`R.drops`). **💰 금화는 예외 — 바닥 드랍·캠 표시 없이 즉시 지갑 적립(5개 상한 미포함·방 만석에도 적립, 2026-07 사용자 지침)**. 캠 아트는 보상과 일치(`dropArt`): **펫알=알·랜덤박스=박스·뜰알=꽃알·🌈무지개동전/무지개알/무지개박스=무지개 아트**(💰 금화는 바닥 드랍이 아니라 즉시 지갑 적립 — 캠 표시·5개 상한 없음, 은화도 유휴적립 그대로라 표현 없음). **캠에서 드랍을 탭하면 바로 수령**(`collectDrop`, 다기기 중복 수집 안전)하고, **수확 버튼이 남은 드랍을 일괄 수령**(`batchCare`). **📦 랜덤박스 드랍은 봉인 그대로 가방(`consum.box`)에 적립**(2026-07 변경 — 예전 즉석 개봉 폐지, 이후 가챠 배너에서 열기), 펫알·뜰알도 봉인, 🌈**무지개동전=`g.rbcoin` +1·무지개알/무지개박스=`g.rbcoin` +5**(1뽑 분량, 캠엔 화려한 무지개 알/박스로 뜨고 보상은 동전). **🪟 수확으로 아이템을 주우면 캠 안에 "작은 창" 요약 팝업**(`showHarvestPopup` — 무엇을 얼마나 받았는지 + **[줍기] 버튼으로 닫음**, 무지개 드랍 섞이면 무지개 테두리). **무지개·알·박스류 드랍 대기 중엔 수확 버튼이 은은한 무지개**(`.rb-wait`, 금화만은 제외)로 변한다. **스폰 순간엔 랜덤박스 배너의 거대 무지개 다이아가 캠에 스르르 나타났다 사라지는 원샷 연출**(`dropSpawnGemFx`). 드랍은 **PiP에서도 보인다** — Document PiP는 **클릭 수집 + 소형 수확 버튼**(`.pp-batch`) 지원, 비디오 PiP는 표시 전용(워커 렌더). 수집 연출은 기존 재사용(`harvestDropFx`/`rewardFly`), `prefers-reduced-motion`·저사양(lite) 정지.
  - **🦋 희귀 손님 + 펫 생동감**: 90초 체크마다 낮은 확률(~5%)로 **나비·잠자리·반딧불 1마리가 캠에 스르르 방문**했다 사라진다(`maybeRareGuest`, 배경효과 미보유 방 포함·로컬 장식·절전 훅 준수). 펫 유휴도 다양화 — 하품/하악질 액센트 빈도·앉기 위주 포즈 가중·가구 상호작용(캣휠·해먹 등) 빈도 상향(PiP로 힐끔거릴 때 볼거리).
- **🍖🍡💊 소비템(효과형)**: 알뜰샵 **소비 탭**에서 사는 소모 아이템(`CONSUM_CATALOG`, 배치 불가·`game.consum`에 수량 저장). **사료·물(은화1)** = 그릇 6h 채움. **고급사료·정수물(은화2)** = 그릇 **12h** 채움(그릇별 `placed.fillMs`로 지속시간·똥정산·행복도 feedFrac 반영). **츄르(금화10, 하루 5개 구매)** = 가방에서 **펫을 골라 애정 +1**(쓰다듬기 쿨다운 무시, 레벨업 보상은 동일, `applyTreat`가 `applyAffectionGain` 공유). **영양제(무료·하루 1개)** = **6시간 수확 수익·드랍 확률 ×1.5**(`game.boost`, `activeBoostMult`가 `effYieldMult`에 곱해져 유휴 은화 3경로에 반영 + `reconcileDrops` 드랍 롤 확률에도 곱, 수확칩에 부스트 표시). 하루 구매 상한은 품목별(`game.buyDay.n[id]`, 츄르5·영양제1). **수확·그릇 급여 자동채움 사료·물 선택**(소비 탭 상단, `harvestPref`/`setHarvestPref`(localStorage) → `pickFill`이 그릇 탭·전체 수확 양쪽에 적용, 선택분 소진 시 기본으로 폴백). 사료·물·고급사료·정수물은 홈에서 그릇 탭/수확으로, 츄르·영양제·염색약은 **가방에서 바로 사용**(🍡 츄르·🎨 염색약은 **펫 정보 화면의 소비템 행**에서도 그 펫에게 바로 사용 가능). **🎨 랜덤 염색약**(`consum.dye`, **알뜰샵 소비 탭 판매 금화100 + 이벤트·쿠폰·선물 지급**): 가방에서 사용하면 **펫 인벤토리 그리드 시트**(`openPetUseSheet` — 츄르·리무버 공용, 펫 탭 즉시 적용 후 시트를 유지한 채 썸네일이 결과 색으로 갱신)가 열리고, 탭한 펫이 **절대색 45색(`DYE_CATALOG`: 레드·핑크·오렌지·옐로우·그린·민트·블루·퍼플·브라운·무채 계열, 색 이름 표기) 중 랜덤**으로 염색된다(`applyDye` — 현재 색 제외, `owned.cats[id].dye`에 색 id 저장·legacy 숫자 하위호환). 색 적용은 `dyedUrl` 단일 소스로 걷기·4방향 스틸·모션 클립·썸네일(그리드·도감·리빌)·친구 캠(petsMeta)·창/비디오 PiP까지 **일관 적용**. **🎨 2색 염색(2026-07 전면 개편, 사용자 지침)**: 펫마다 **바탕색(몸)=염색1(`dye`)·얼룩·눈=염색2(`dye2`)** 두 영역을 독립 염색한다. 두 영역은 CSS/SVG 필터로 못 나눠(루미넌스 LUT 한계) **펫별 색분할 마스크 + 캔버스 베이크**로 처리 — 스프라이트를 `getImageData`로 **아웃라인/몸/얼룩 3영역 분할**(아웃라인=아주 어둡고 저채도·몸=south 최빈 비외곽색 근처·얼룩=나머지) 후 몸→염색1·얼룩(눈 포함)→염색2로 픽셀 재색해 dataURL로 스왑(`_bakeRecolor`/`ensureSegProfile`/`dyedUrl`, 사용된 색조합만 지연 베이크·캐시). 한 영역만 지정하면 그 영역만 물들고 나머지는 원본(자연 얼룩/눈) 유지. **루미넌스가 아니라 펫 팔레트 기준이라 검은 고양이·회색 펫도 몸이 물들고, 흰 고양이의 눈 같은 accent도 염색2로 바뀐다.** 색 정체성(색상환·이름)은 그대로. **염색약(금화100) 1개=한 영역**: 펫 탭 → 바탕/얼룩 선택 → 그 영역만 랜덤(`applyDyeZone`). 리무버는 두 영역 모두 복원. PIL 프로토타입 + 실제 Chromium(Playwright)으로 라이트/다크 검수 확정. legacy 숫자 저장값은 미염색 처리(2색 개편으로 폐지). 제거는 **🧴 염색 리무버**(`consum.dye_remover`, 은빛 병 도트 — **알뜰샵 소비 탭 판매 금화200 + 이벤트·쿠폰·선물 지급**)로만: 가방에서 염색된 펫을 골라 1개 소모(`applyDyeRemover`, 무료 지우기 없음). 병 아이콘=무지개 3단 물약 도트(`M_DYE`, PIL 검수 — 리무버는 같은 병에 투명 액체 팔레트).
- **🫶 친구 집 방문 보상**: 친구 집 **좋아요 시 방문자 +10 은화**(하루 3회, `likeGiven`), 집주인은 **받은 좋아요 마일스톤(10·25·50·100·200·500)마다 그 수만큼 금화**(`likeMilestone`).
- **🔥 로그인 스트릭**(`game.streak`): 연속 출석 **3=+50·7=+100+금화2·14=+200+금화3·30=+400+금화6·60=+600+금화10·100=+1000+금화15**(놓치면 리셋, 이후 매100일 반복). 미션 화면 상단 배지에 연속일·다음 보상까지 표시(`autoClaimAttend`·순수 `loginStreakReward`).
- **🐾 도감**(`openPetDex`, **더보기 → 도감** 타일, 책+발자국 픽셀 아이콘 `dexSvg`): **[펫 | 아이템] 상위 탭**(`_dexKind`). **펫 도감** = 전체 펫 그리드에서 **보유=컬러/미보유=실루엣**, 수집 **N/총·%**, 애정 하트, 종별 탭. **전체 25/50/75% + 종별 완성 마일스톤 보상**(은화·금화, `game.dexClaims`에 멱등, 100%는 완성 업적). **📖 아이템 도감**(`buildItemDex`/`dexItemList`) = **기구물(케어·휴식·놀이·장식 하위그룹)·벽지·바닥·배경효과**(소비 제외), 분류탭(`_dexItemCat`)·보유=컬러/미보유=실루엣·수집률%. 기구물=도트 SVG(`furnSvg`)·벽지/바닥=패턴 스와치(`wallCss`/`floorCss`)·배경효과=`bgfxThumb`. 보유 판정 `itemQty`/`ownsWall`/`ownsFloor`/`ownsBgfx`. `dexProgress`.
- **🛒 알뜰샵(임시 개편)**: 펫·기구물·벽지·바닥은 **가챠(랜덤박스)로만** 획득 — 알뜰샵 서브탭에서 **은화 구매 탭(펫·가구·벽지·바닥)을 제거**하고 **이벤트 · 펫알 · 랜덤박스 · 무지개 · 소비** 5탭으로 재구성(`SHOP_SUBS`/`shopSubsegHtml` — 가챠 내부 서브탭을 상단으로 승격, 펫알·랜덤박스 분리·이벤트=한정픽업 뜰알·무지개=금화). 렌더 코드는 보존 — 추후 **금화 로테이션 판매**로 재도입 예정. **이달의 할인펫 배너도 소식에서 잠시 제거**(`featuredCatId` 로직 보존).
- **❤ 펫 애정도**(`owned.cats[id].affection`): 방·캠에서 **펫을 탭해 쓰다듬기 +1** — **펫별 하루 1번만**(RTDB `owned.cats[id].pettedAt`로 지속, `PET_COOLDOWN_MS` 24h). **성공 시 하트 연출 + 은화 +10**(`PET_PET_REWARD`, **은화는 하루 3마리까지만**·`PET_DAILY_CAP` — 4마리째부터는 애정만 오르고 은화 없음, 도배 억제) — 은화가 캠 우상단 지갑 카운터로 날아가 카운트업(`rewardFly`). 좋아요와 동일한 픽셀 하트 상승 + 작은 하트 뿅 팝(`heartFx`+`likeBurst`); 쿨다운 중엔 하트·보상 없이 남은 시간만 안내. 임계는 **등급별 계단**(2026-07 개편, `util.affTiers` = base **2/5/12/24/40** × 등급배수 1/1.25/1.5/2/2.5/**신화3/한정4** — 예: 한정 만렙 160, **최대 5레벨**) → 애정도는 "명품백"처럼 **어렵게 관리되는 프레스티지**(펫 획득은 쉽게·애정은 천천히). 우리집 펫 카드에 **애정 레벨 배지 `Lv.N`(항상 표시, `.clv`)**. **레벨업 시 캠에 분홍 픽셀 "UP!" 두둥 연출**(하트 옆 팝→상승, `affLevelFx`/`M_UP`/`.lvlup`) + **레벨별 소보상 은화(20·30·50·80·100, `util.affLevelReward` — 다중 레벨 점프도 걸친 레벨 전부 합산, 수령 마커 `owned.cats[id].affRw`로 재지급 차단)**. **레벨5(만렙) 최초 도달 시 금화 +5**(자동 멱등). **🥚 가챠 중복 펫은 은화 대신 그 펫의 애정 경험치**(`dupAffOf` = 등급별 2/3/4/6/8/**신화12/한정16**, 만렙 펫만 은화 10% 폴백 `petDupRefund` — 지급 `grantPetDup`·리빌 표기 `petDupPreview` "♥ 애정 +N"). **🌈 신화↑(신화·한정) 중복은 무지개동전도 함께**(신화 **+1**·한정 **+2**, `dupRbcOf` — 만렙이면 동전만, 신화↑ 아이템 중복은 은화 대신 동전). **💗 펫 모션 애정 해금(펫별 동적 사다리 · 전 등급, 2026-07 사용자 지침)**: 보유 모션의 기본 레벨(`CLIP_AFF_REQ`)을 `petClipAff`가 **"최저 보유=Lv1"로 시프트**해 펫마다 Lv1~5 사다리를 자동 구성 — 기본 7종만 가진 펫은 **Lv1=유휴·앉기·먹기·마시기 / Lv2=식빵·하품 / Lv4=하악질**(애정이 처음 생기면 생동감이 켜짐, Lv0=기존 스틸/걷기만), zip 제공 모션(점프·질주·멍멍 등)이 있으면 그 사이 레벨에 자동 배치된다(`clipAffLocked` — `hasClip` 게이트, 미달 시 스틸 폴백·펫 정보 시트에 **펫별 해금 표기 `petAffLadderHtml`**, 개발자 모션 관리는 우회+잠금 표기). **전 등급 적용**(등급별 임계 `affTiers`라 저등급이 더 빨리 도달). **🛋️ 가구 상호작용 모션**: 가구 도착 시 `furnClip`의 `FURN_CLIP_MAP`이 **그 펫이 보유한 클립일 때만** 재생(미보유=스틸 폴백, east 클립은 west 자리에서 `clipFlipDir` flip — 침대류→꾹꾹이·장난감→톡톡·어항류→물끄러미·스크래처→스크래칭·그루밍아치→기지개·터널→실룩 매핑은 인프라로 유지). **이 6종 모션의 규칙 생성분(신화·한정 16종)은 2026-07-10 전량 삭제** — 앞으로는 zip이 그 모션을 제공하는 펫만 재생(사용자 지침: 기본 7종 외 모션은 규칙 생성 금지). 비상호작용이던 **간식단지·캣그라스(→먹기)·테더볼·바람개비·버블머신 5종 상호작용화는 유지**(INTERACTIVE_FURN — 먹기·마시기는 기본 7종이라 전 펫 재생, 행복도 enrichment 포함). 기존 애정치는 **수치 유지·레벨만 새 임계로 재해석**(1회 마이그레이션 `migrateAffRwIfNeeded`, `game.affV=2` — 구 임계 기준 레벨을 `affRw`에 기록해 재도달 이중 지급 방지). `affectionLevel(aff,tier)`/`bumpAffection`. **💗 코스메틱(슬롯=레벨 오픈 + 아이템=획득 필요, 전 등급)**: 펫별 **펫효과 슬롯=Lv3·모자 슬롯=Lv5**에 열리지만, **장착하려면 그 아이템을 보유**해야 한다(`owned.hats`/`owned.petfx`, own-once — 레벨만으론 착용 불가). **✨ 펫효과 3종**(나비·반딧불·**무지개꽃**(`M_PETFX_RBFLOW` — 빙글 트월 `cbtwirl` 모션), 추후 추가)과 **🧢 모자 3종**(밀짚·베레·파티 `HAT_CATALOG`)은 **전부 한정(exclusive) 등급**(`PETFX_TIER`/`HAT_TIER`) — **2026-07 무지개동전 개편으로 boxPool 편입**: 기본 랜덤박스 0.2%·무지개박스 50%로 출현(한정 중복=무지개동전 +2) + 이벤트·쿠폰·선물함·개발자 지급 병행(도감 «펫 코스메틱» 수집축·NEW 배지 유지). 장착=펫 정보 시트(`setPetCosm` → `owned.cats[id].cosm={hat,buddy}`, 미보유 칩은 흐림 표기), 캠 3무대(dock·홈·친구)+Document/비디오 PiP(워커 전사)에 표시(버디=경로 `cbpath`+날갯짓 `cbbob`/발광 `cbglow` 다층 모션). 머리 앵커는 스프라이트 상단 여백 실측(`measureHeadPad` → `--hp`). 선물함·전체선물·쿠폰은 `type:'hat'/'petfx'` 지급 지원. **애정 과시(어필)**: 친구 캠 액터 머리 위 **♥Lv 배지(Lv3+)**(homeCam `petsMeta`={lv,cosm} — 수치는 비공개), 펫 카드 **만렙(Lv5) 골드 하트+★**(`.clv.lv5`). (랭킹 카드의 '애정 N' 칩은 2026-07 제거 — 표시 없이 `rankings.aff` 기록만 유지.) 절전 규칙(reduced-motion·lite·시트 가림) 준수.
- **🦊 가변 모션(펫별 제각각 모션, 2026-07 신설)**: 펫 zip에 담긴 모션은 펫마다 다를 수 있고(개과=멍멍 bark, 고양이과=점프·질주·식빵 등), `build_pets.py`가 자동 인식·합성해 반영한다(`MOTION_MAP` 표준 클립키 정규화 + 방향 자동 선택 `PET_SPRITES.clipDirs` — east만 있는 클립은 west에서 좌우 플립 재생). 규칙 생성은 **기본 모션 7종(유휴·앉기·식빵·먹기·마시기·하품·하악질)이 zip에 없을 때만** 보충하고, 그 외 모션은 zip 제공분만 반영(없으면 없는 대로 — 엔진 폴백). **애정 해금 레벨은 펫별 동적 사다리**(`petClipAff` — 그 펫이 보유한 모션 중 최저 레벨이 Lv1이 되도록 시프트, 펫 정보 시트가 `petAffLadderHtml`로 레벨별 해금 모션을 펫마다 표기). 첫 사례 **구미호(fox_nine, 한정)** = 9모션(Lv1 유휴·앉기·먹기·마시기 / Lv2 질주·식빵·하품 / Lv3 점프 / Lv4 하악질). 제작 규칙은 [pet-asset-pipeline.md](pet-asset-pipeline.md) 가변 모션 절.
- **🐾 우리집 펫 = 수집형 인벤토리 그리드**(수백 마리 관리): 홈 탭의 보유 펫 목록을 **가로 스크롤 한 줄 → 5열 그리드(세로로 길게 나열, 시트가 세로 스크롤)** 로 리메이크(`catHomeHtml`의 `.catchips`). 축소 타일 = **정면 정지 초상**(정렬 그리드는 정적이 깔끔·성능↑, 걷는 모습은 위 방 무대에) + 이름(등급색) + `♥Lv.N`. 상태는 텍스트 대신 **테두리+뱃지**(이 방=강조 테두리+체크, 다른 방=파랑 테두리+방이름 뱃지, 대기=기본)이며, **펫이 담긴 초상 rect(`.cpic`) 테두리에 등급색**(일반=중립 회색·고급 초록·희귀 파랑·특별 보라·전설 금·신화 핑크·**한정 무지개** `border-image`)을 두께감 있게 입혀 등급을 한눈에 본다. 등급 링은 공통 클래스 `.tbring`으로 **도감(보유 셀)·알뜰샵·개발자 카드에도 일관 적용**되고, 색약 대응으로 **등급명 텍스트 배지**(`.ptier`, 일반은 생략)를 카드에 단다. 탭=이 방으로 배치/대기(`toggleActiveCat`), 코너 ✎=이름 짓기, **ⓘ=펫 상세 시트**(`openPetInfo`: 등급·애정 진행바·획득일·종·방 상태 + 상세에서 바로 쓰다듬기). **도감식 종류(species) 탭**(전체·고양이·강아지…·개수 배지, 2종↑일 때, `homeSpeciesTabs`/`setHomeSpecies`/`_homeSpecies`)으로 나눠 보고, **정렬**은 `이 방 우선(기본)·최신순·애정도순·등급순·이름순`(`sortOwnedPets`). **기본 '이 방 우선'** = 현재 보고 있는 방에 배치된 펫을 최상단에(→ 즐겨찾기 → 최신), **정렬을 고르면 배치 우선이 풀리고 순수 조건 정렬**(즐겨찾기만 먼저). 방을 전환하면 그 방 배치 기준으로 자동 재정렬(orderSig에 정렬결과 포함). **탭/정렬 선택은 새로고침 후에도 유지**(`lsGet`/`lsSet`). 그리드는 **4행까지 보이고 초과분은 내부 스크롤**(`fitPetGridRows`가 카드 높이×4로 정확히 제한, `.catchips.scroll4`). **성능**: 그리드는 `renderPetGrid`가 **타일 단위로 메모이즈**(`petTileHtml`/`petTileSig`, `data-tsig`)하고 시트 리프레시 시 `#petGrid` 노드를 보존해, 펫이 수백이어도 **바뀐 타일만 교체**(펫 탭=1개, 무관한 변화=0개)한다.
- **🌟 이달의 펫(시즌)**: 매월(KST) **은화로 살 수 있는 등급(특별 미만) 중 한 마리**가 자동 선정돼 알뜰샵 펫 탭 상단 배너·카드 리본으로 강조되고 **은화 20% 할인**으로 살 수 있음(모든 사용자 동일·매월 로테이션, 순수 `featuredPetOfMonth`/`featuredCatId`·`catBuyPrice`, 할인가는 월 기준 결정적이라 결제 트랜잭션과 표시가 일치). 미보유 시 "이번 달만 N% 할인" FOMO 훅. **선정 우선순위**: ① **개발자 수동 선정**(전역 `config/featuredPet/{monthKey}=id`, 관리자 계정만 쓰기·전체 읽기, `loadFeaturedPet` 구독) → ② 없으면 **월키 해시 자동 — 미보유 펫 우선**(이미 보유한 펫이 걸리면 할인 훅이 죽어서, 각자 미보유 풀에서 선정 → **사용자마다 다를 수 있음**·다 보유 시 전체 풀 폴백·이달의 펫을 사면 다음 렌더에서 새 미보유 펫이 자동 선정). 해시 자동은 후보 목록 길이에 의존해 **펫 추가/삭제·보유 변화 시 자동 선정이 바뀌므로**, 전체 공통으로 고정하려면 개발자 모드 **이달의 펫 선정**(`openDevFeatured`)에서 직접 고른다(수동 선정은 보유 여부 무시·전원 동일).
- **🎁 친구 선물(mailbox)**: 친구 집(`openFriendHome`)에서 **친구에게만** 선물 발신 — ① **펫알 선물**(은화 100 지불 → 친구가 받아 가방 펫알+1 → 열어 랜덤 펫, 기존 가챠 재사용) · ② **무료 응원 선물**(무료, 물/사료/은화 랜덤 1개, 가중 `rollFreeGift`: food35/water35/coins10=30 — **금화는 제외**). **친구당 하루 1번 + 전체 하루 5번**(`game.mail.freeTo[일자][uid]` + `game.mail.free[일자]`). 펫알 선물은 전체 하루 5회(`game.mail.egg[일자]`). 도착은 **크로스유저 노드 `users/{수령자}/mailbox/{발신자}/{gid}`**(`game`은 소유자 전용이라 별도 노드) → 더보기 **선물함**의 "친구가 보낸 선물" 섹션에서 받기(`claimMailGift`/`claimAllMail`, 배지=코드선물+친구선물). **보안**: 규칙에서 **친구 관계 검증**(친구만 write) + 엔트리 **`.validate` 상한**(coins≤10·consum≤3·key∈{egg,water,food}·from=auth.uid, **금화 타입 자체 차단**)으로 크로스유저 통화 위조·민팅 방지(`sendPetEggGift`/`sendFreeGift`).
- **📢 소식 화면**(`openNews`, **좌상단 브랜드(알뜰) 아이콘** 또는 더보기 → 소식): **🔔 알림**(안 받은 선물=코드보상+친구선물 → 선물함으로), **🎉 이벤트**(이달의 펫 배너 → 알뜰샵), **📢 공지사항**(업데이트 내역 + 쿠폰 4종 표 — 이미 쓴 코드는 흐리게 '사용완료'). 아이콘은 픽셀 아트(종 `bellSvg`·확성기 `megaSvg`·선물 `giftSvg`·시즌 할인 세일태그 `seasonSvg` 등). 공지 항목은 **왼쪽에 픽셀 아이콘**(`noticeIcon`이 제목 키워드로 선택: 선물→`giftSvg`·할인/시즌→`seasonSvg`·그 외 `megaSvg`)을 두고 제목 앞 이모지는 `noticeTitle`로 제거. 안 본 공지는 로컬(`newsSeenAt`)로 판정하고, **좌상단 브랜드 아이콘**·더보기 소식·소식 화면의 알림 뱃지는 **안 받은 선물(코드+친구)+안 본 공지**(`newsUnread`/`giftUnread`)를 함께 센다. *아이콘 역할 정리: 캠 화면 **은화 아이콘 = 알뜰홈**(`coinTap`→`openCatHouse`), **브랜드 아이콘 = 소식**.*
- **📋 미션 화면**(`openMissions`, 더보기 → 미션, 체크리스트 픽셀 아이콘 `missionSvg`): 일일·내 미션·주간·월간·업적·로그인 스트릭을 한곳에 모음(알뜰홈 미션 탭과 오늘 홈 랜딩의 미션 상세를 일원화 — 오늘 홈은 진행 요약 + '미션 보러가기'만 남김).
- **dock**: 얇은 스트립(은화 배지 탭 시 **소식** 화면) ↔ 숨김(더보기 설정에서 토글). 은화 배지엔 **알림 뱃지**가 뜨고, dock을 숨겨도 **더보기 → 소식**으로 접근 가능.
- **동물 <!--@gen:cats-short-->148종(고등어·뒤뚱·길냥·네로·하양·복슬이·검정얼룩이·골목대장·삼삼이·황토·폴드·보라·초코·아깽이·핑크·고랑이·갈기냥·페르시안(흰색)·백호·블루·얼룩이·시고르자브·블랙팬서·백구·시바·코기·달마시안·닥스훈트·불독·인절미·스탠다드푸들·비글·숙희·도베르만·퍼그·저먼셰퍼드·보더콜리·스피츠·잭러셀테리어·레브라도·차우차우·카디건코기·그레이하운드·시츄·세인트버나드·보스턴테리어·바셋하운드·해피·웰시테리어·파피용·뉴펀들랜드·비어디드콜리·보더콜리·로트와일러·포인터·파라오하운드·웨스트하이랜더테리어·바이마라너·콜리·잉글리시불독·키스혼드·프렌치불독·요크셔테리어·토이푸들·셰틀랜드십독·미니어처핀셔·슈나우저·골든두들·버니즈마운틴독·캐벌리어스파니엘·아키타·휘핏·올드잉글리시쉽독·비즐라·잉글리시셰터·진돗개·차이니즈크레스티드·스코티시테리어·포메라니안·샤페이·그레이트데인·불테리어·복서·로디지안리지백·아이리시세터·에어데일테일러·사모예드·시베리안허스키·고등어·칼리코·하양·치즈·턱시도·샴·벵갈·러시안블루·스코티시폴드·까망·설렁·페르시안(회색)·메인쿤(갈색태비)·아메리칸숏헤어(실버태비)·랙돌(포인트)·터키시앙고라(흰색)·먼치킨(삼색)·노르웨이숲(갈색)·봄베이(검정)·아비시니안(갈색)·스핑크스(핑크)·브리티시숏헤어(그레이)·벵갈(스노우)·장모 삼색·토터셸(카오스)·샴(초콜릿포인트)·코니시렉스·오시캣·셀커크렉스·코랫·맹크스·아메리칸컬·데본렉스·터키시반(반무늬)·밥테일·버미즈·히말라얀·크림태비·라일락·소말리·삵·시라소니·치타·재규어·퓨마·눈표범·카라칼·표범·흑표범·오셀롯·모래고양이·메인쿤(블랙스모크)·메인쿤(레드태비)·벵갈(실버)·피더볼드·토이거·싱가푸라·하바나브라운·라가머핀·구미호)<!--@gen:end-->**, **가구 28종**(방석·밥그릇·물그릇·캣타워·스크래처·배변패드·펫하우스·화분·캣휠·러그·창문·어항·벽난로·선풍기·해먹·낚싯대장난감·벽시계·행잉플랜트·모빌·방울공·액자·벽선반·거울·네온사인·벽등·가랜드·포스터·태피스트리 — **러그=바닥 아이템**(위에 가구를 올릴 수 있음)·**액자/벽선반/거울/네온/벽등=벽 가구**, **캠 전용 연출**: 창문 구름 흐름·어항 금붕어·벽난로 불꽃 일렁임·선풍기 날개 회전·해먹/낚싯대/벽시계/행잉플랜트/모빌/방울공 살랑임 — **가구도 등급**(`ITEM_TIER`): 러그=희귀·어항=특별·창문 등 장식가구=전설. **특별↑ 가구·바닥·벽지는 알뜰샵 구매 불가, 랜덤박스에서만** 획득(`isGachaOnlyItem/Floor/Wall`=tier≥특별 기본, **개발자 '기구물 관리'의 가챠전용 토글 `config/*/{id}.gacha`로 개별 오버라이드** 가능). **가챠전용이면 알뜰샵 판매 탭에서 아예 숨김**(펫도 동일 — 펫 수정의 가챠전용 토글 `catalogPets.gachaOnly`, `isGachaOnlyCat`), 가챠전용이 아니면 등급에 맞춰 은화 판매. **어느 쪽이든 가챠(펫알/랜덤박스) 풀엔 항상 포함**. 가구는 **종당 최대 12개**(`ITEM_MAX_QTY`) — 가챠·드랍으로 초과 획득 시 등급가의 **10% 은화 환급**, 알뜰샵 구매는 캡에서 차단), **벽지 10종**(기본·하늘·벚꽃·민트·별밤·살구·노을·숲·바다·라벤더, 알뜰샵 벽지 탭에서 구매·적용). 고양이 알뜰샵 가격(은화)은 **등급·확률에 맞춰 산정**(일반50·고급100·희귀200·특별400·전설800·신화1500·**한정=알뜰샵 비매(오직 '뜰알' 한정 픽업에서만 획득)**, `TIER_PRICE`가 `CAT_TIER` 단일 소스로 자동 산정 — 알 100은화+금화·환급 대비 흔한 등급은 싸게·희귀는 비싸게). **등급 체계**: 일반·고급·희귀·특별·전설·**신화(`limited`, 핑크 텍스트/연출)**·**한정(`exclusive`, 무지개 텍스트/연출·최상위, `p:0`으로 기본 펫알/박스 미출현 — 오직 '뜰알'(한정 픽업) `DDEUL_TIERS`에서 활성 펫만 출현·알뜰샵 판매 제외)**. 상단 dock은 **웹캠 방**(위에서 아래로 내려다보는 뷰, 바닥 넓게·벽지 좁게)으로 벽지 배경 + 배치 가구(배경) + 걷는 고양이를 함께 보여주며, **LIVE 배지는 좌측 상단**. 가구는 **원근**을 적용해 앞(격자 **아랫줄**)일수록 크고 아래, 뒤(격자 **윗줄**)일수록 작고 위로 보이며 앞 가구가 뒤 가구를 가림. **배치 탭**엔 현재 배치를 실제 방으로 보여주는 **미니 웹캠 프리뷰**가 있어 방향을 확인할 수 있음(격자 위=방 뒤, 아래=방 앞).
- 고양이는 걷기 외에 **앉기·식빵·낮잠 포즈**(SVG 고양이) 또는 **멈춰 앞/뒤/좌/우 보며 쉬기**(스프라이트 고양이), 개별적으로 속도·방향·유휴가 랜덤이라 제각기 움직이며, 배치된 **가구로 다가가 종류별로 반응하며 몇 초간 머무름**(캣타워=3층 중 한 층에 올라가 정면 보며 쉼·방석=위에 잠시 올라감·밥그릇=뒤에서 앉아 먹음·스크래처=옆에서 머무름·**펫하우스=출입구 안에 들어가 정면 보며 아늑하게 오래 쉼**). 자유 로밍 펫끼리 같은 배치칸이 겹치면 비켜가고(`separatePets`), **가구로 가는(goal)·앉은(pause) 펫은 겹침분리에서 제외**돼 다른 펫에 막히지 않고 곧장 앉습니다.
- **펫 원근(캠/방)**: 고양이도 가구처럼 **좌우뿐 아니라 앞뒤(깊이)로도 이동**해 **가까우면 크고 앞으로, 멀면 작고 뒤로** 보임(근거리 확대는 넉넉히·원거리 축소는 적당히). **정면 캠 특성상 앞뒤(원근) 배회 속도는 좌우보다 훨씬 느리게**(`stepActors`의 `vz`, 전체 깊이 이동에 25초+) 잡아 "앞뒤로 금방금방 움직이는" 느낌을 없앰. 깊이 z-index가 가구와 같은 척도라 **가구를 가리기도·가려지기도** 함. 가구 상호작용 시엔 그 가구의 깊이에 맞춰 서고, **펫을 세로로 드래그하면 앞뒤로 이동**(위=멀리·작게, 아래=가까이·크게), 가로 드래그는 좌우 이동. (`cats.js` `applyDepth`/`depthScale`/`petScale`, `PET_NEAR_SCALE`/`PET_FAR_SCALE`.)
- **🍚 다마고치 사료·물·배변 루프**: 알뜰샵 **소비 탭**에서 **사료·물**(각 1 은화, 배치 불가) 구매 → 홈 화면에서 **밥그릇·물그릇을 탭**하면 사료/물 1개를 써서 채워짐. 채운 지 **3시간 뒤 그릇이 비워지고**, 비운 그릇 하나당 **화장실에 똥 1개**가 쌓임. **똥을 탭하면 +2 은화**. 홈·캠 **우측 상단 "수확" 버튼**으로 **모든 방 일괄 처리**(전 방 그릇 채우고 똥 모두 치우기 + 자동수익 수확). 밥·물그릇·화장실은 **방마다 종류별 1개만 배치**(1개면 효율 최대 — 도배해도 행복도·수확엔 반영 안 됨).
- **고양이 이름 짓기**: 홈 "우리집 펫" 칩의 **✎ 버튼**으로 고양이별 이름을 지을 수 있음(최대 12자, `owned.cats[id].name`). 지은 이름도 **등급 색**(신화=핑크·한정=무지개)으로 표기됨.
- **개발자 모드 진입**: 개발자 이메일(`isDev`) 계정은 **더보기 그리드에 무지개알 '개발자' 타일**이 노출되고, 탭하면 **개발자 모드 시트**(`openDevModeSheet`)에서 켜기/끄기 + 펫알·박스 확률 설정 + 펫 관리로 진입(설정 시트에는 더 이상 없음).
- **개발자 펫 관리(앱 내)**: 개발자 모드에서 **펫 관리** — 전체 펫 목록을 **도감식 종류(species) 탭 + 등급별 섹션**(`_devPetSpecies`/`setDevPetSpecies`, 삭제됨은 맨 끝 섹션)으로 훑어 골라 관리한다. **추가**=PixelLab zip 업로드로 새 펫 등록, **수정**=이름·분류·등급·크기 변경 + zip 재업로드 시 디자인 교체(정적 펫도 오버라이드), **삭제**=소프트 삭제(이미지·정의 보존, 알뜰샵/가챠/목록에서만 숨김 · 관리 화면엔 회색 "삭제됨"), 삭제된 펫은 **복구**로 재활성화. **목록 각 행에서 등급 셀렉트 + 가챠전용 토글로 바로 변경**(기구물 관리와 동일 · `setPetTier`/`setPetGacha`, 전역 `catalogPets/{id}.tier`/`.gachaOnly` 오버라이드). 앱에서 바로 처리(JSZip+canvas)해 RTDB `catalogPets` 전역 저장 → 모든 사용자 반영(재배포 불필요). 쓰기는 개발자 이메일만(RTDB 규칙). 정적 파이프라인은 공식 펫용으로 병행.
- **프로모 코드**: 더보기 → 코드 입력. 각 코드는 1회 제한이지만 **앞에 `@`를 붙이면 무제한** 사용(개발자·테스트용).
- **코드 입력**(더보기 → 설정): **2026-07 쿠폰 전량 삭제**(`PROMO_CODES={}`, @무제한 규칙도 폐지) — 입력 UI·선물함 적립 흐름은 유지되고 새 이벤트 코드는 운영 시점에 추가한다(일반=코드당 1회 멱등 `g.codes`, 개발자=무제한). 소식 화면 쿠폰 목록은 빈 상태 문구 표시.
- **🎁 선물함 · 🎒 가방**(더보기 그리드): 코드 보상은 **선물함**(랜덤박스 도트를 선물상자 색으로 재채색한 `giftSvg` 아이콘)에 쌓이고, **받기/모두 받기** 하면 은화는 잔액으로·아이템은 **가방**으로 이동. **가방**에서 보유한 **펫알·랜덤박스**는 가챠샵으로 안내해 오픈(`consum.egg/box`, `game.gifts`). (무지개알·무지개박스 소비템은 2026-07 폐지 — 무지개동전 뽑기로 전환, 잔여분은 마이그레이션 회수.) 선물함 행은 아이콘·이름과 함께 **메시지**(`gift.msg`)도 표시.
- **🎉 회원가입 축하 선물**: 신규 계정 첫 진입 시 **은화 100 + 펫알 1개**를 선물함에 넣고 "알뜰 회원가입을 축하 선물이에요! 🎉" 메시지를 남김(`grantWelcomeGift`, `users/{uid}/welcomeGift` 플래그로 1회 멱등).
- **🏆 랭킹(집 좋아요 TOP10)**: 더보기 **트로피 타일** → `openRanking`. 전체 사용자 중 집(펫캠) **좋아요 수** 상위 10 — **1·2·3등 금·은·동 메달 단상**, 4~10등 리스트(아바타·닉네임·❤수). 항목 탭=그 사람 **캠만** 방문(할일은 친구 사이일 때만). 공용 경량 노드 `rankings/{uid}`(소유자만 유지·`writeMyRanking`)만 읽어 정렬, 상위권 사진만 지연 로드. **비공개 프로필**은 은화+'알뜰'로 익명 등장.
- **🌈 한정 픽업 배너 = 살아있는 픽셀 숲 씬**(가챠 탭 상단): 이달의 펫 배너처럼 가챠 탭 맨 위에 노출 — **펫1·펫2** 라벨(한정 무지개색) + 디테일 픽셀아트 씬. **하늘**엔 서로 다른 모양·색·속도의 **구름이 바람에 흐르고**, **무지개**는 넓고 길고 **연하게**(진입 1초 뒤 사르르). **뜰**엔 **흙이 드러난 불규칙 지형**·**꽃·풀이 제각각 바람에 살랑**·**원근 나무**(뒤 소나무·활엽수 + 앞 큰 나무, 잎이 흔들림)와 가운데 **로그인 알(egg-garden)**. **픽업 펫**은 **캠 배치 시스템을 재사용**(`#pkStage`=필드 크기 미니 캠, `.cd-room` 원근 공유 + `buildActors`의 `data-noprops`로 사용자 가구 제외)해 양옆에서 걸어와 **캠처럼 자유 배회**(원근 배율로 2배↑ 크게). 픽업 대상은 `LIMITED_PICKUP` 배열(현재 흑표범·퓨마), 헬퍼 `cloudSvg`/`treeTopSvg`/`pineSvg`/`flowerSvg`/`tuftSvg`/`rainbowArcSvg`, `prefers-reduced-motion` 시 정지. *현재 한정(exclusive)은 가챠 풀에서 제외라 배너는 픽업 소개용 표시.*
- **가챠(뽑기)**: 알뜰샵 가챠 탭(구 '이벤트') 안은 **뜰알·펫알·랜덤박스·무지개 4개 서브탭**(`_gachaTab`/`gachaTabHtml`·선택 유지)으로 나뉘어 종류별로 뽑는다(뜰알 탭=한정 픽업 배너, 각 탭 하단에 보유분 열기 — **탭별 전용 배너**: 뜰알=한정 픽업 낮 씬·펫알=노을(연못)·**랜덤박스=노을(선물상자 무더기+트윙클, `sunsetSceneHtml(mode,'box')`)**·무지개=밤). **펫알**(→고양이)·**랜덤박스**(→가구) 각 100 은화. 구매 시 화면 중앙 오픈 연출 → 등급(일반/고급/희귀/특별/전설/신화/한정, 확률 **45/30/15/6/3/0.8/0.2%** — 2026-07 신화 1→0.8·한정 0.2 신설) 랜덤 보상. **1뽑에도 탭별 배너 기반 배경**(`fxSceneBg` — 무지개=밤·뜰알=노을·박스=보물, 메인 화면이 뒤에 비치지 않음). 등급별 확률 고지는 **설정 → `확률 안내`**(`openProbInfoSheet`)에서 모든 뽑기(펫알·랜덤박스·뜰알·무지개)를 한 화면에 공시(배너/탭에는 미표기). **알뜰샵 고양이·가구는 등급 오름차순 정렬**이며, **특별(epic) 이상은 은화 구매 불가 → 펫은 `🥚 펫알 전용`·가구는 `🎁 랜덤박스 전용`(가챠로만)**. 열 때마다 **금화 1개**, **중복 고양이는 은화 대신 그 펫의 애정 경험치**(`grantPetDup` — 등급별 `dupAffOf`, 리빌에 "♥ 애정 +N"·레벨업 은화 병기, **애정 만렙 펫만 등급가의 10% 은화 폴백**. 스킨 중복·가구 초과는 은화 10% 그대로). **✨🌈 무지개알·무지개박스(2026-07 개편)**: 무지개 탭 안 **[알|박스] 서브탭**(`_rbSub`) + **밤 배너 씬 적용**(픽업 펫 배치만 제외). **무지개동전 5개로 1뽑**(10연 50, `RAINBOW_PRICE_RBC`) — 확률 **신화 50%·한정 50%**(`RAINBOW_TIERS`), **미공개 한정 펫·아이템 전부 출현**(`rainbowCatTierMap`=effCatTier). **무지개동전(rbcoin)은 한정 등급을 중복 획득하면 +1**(전 가챠 경로, 리빌·10연 카드에 '무지개동전' 표기). **가챠 중복 펫이 애정 레벨업하면 하트 뿅뿅+UP! 연출**(`fxAffLvUpHtml`). **오픈 연출은 등급색 기반 픽셀아트**로 개편 — 무대 전체가 등급색으로 틴트되어 파티클·광선·반짝임·프레임이 등급색을 따르고 **등급↑일수록 화려**, 무지개는 열기 전 **무지갯빛 반짝임**. 알이 갈라지는/박스가 열리는 순간의 빛은 **화려한 도트 방사 버스트(십자 아닌 12갈래)+은은한 디더 오오라**가 **바깥으로 살짝씩 퍼져나가는(발산)** 연출(회전 아님), 열고 나면 **펫 주변에 작고 은은한 오오라 + 둘레 트윙클 반짝임**(화면 정중앙 아님, 펫을 따라감). **🌈 처음 획득**(중복 아님)한 펫/아이템이면 등장 시 그 위에 **움직이는 무지개색 픽셀 "NEW" 배지**가 뜨고 **글자마다 물결처럼 위아래로 흔들린다**(`gachaNew`=지급 전 보유 판정 → `newBadgeSvg`/`pxSvg` RAINBOW + `.fx-new-ch`/`fxnewbob`, 무지개알/무지개박스 포함).

## 공통 기반

두 모드가 함께 쓰는 토대입니다.

- **로그인 · 인증**: 이메일/비밀번호(Firebase Auth). 로그인 화면 하단에 **아이디 저장 · 자동 로그인** 흰검 토글(`localStorage` 저장·로그아웃 후에도 유지, 기본 on). 자동 로그인 on=`LOCAL` 지속성(앱 재실행에도 로그인 유지)·off=`SESSION`(완전 종료 시 재로그인), 아이디 저장 on이면 이메일 자동 입력(`authOptGet`/`beforeAuth`). **아이디 · 비밀번호 찾기**: 로그인 화면 링크 → **로그인 도움 시트**(`openLoginHelpSheet`) — 아이디는 곧 가입 이메일이라 안내로 대체하고, 이메일을 넣어 **비밀번호 재설정 메일**(`sendPasswordReset` → `sendPasswordResetEmail`)을 받는다. (실제 발송엔 Firebase Console 승인 도메인·재설정 이메일 템플릿 설정 필요.)
- **워크스페이스(개인 프로필/그룹)**: 모든 데이터(`ws/{wsId}`)와 멤버·초대코드의 격리 단위. **개인 프로필**(`ws_{uid}`, 항상 존재)은 나만 보는 개인 가계부·할일, 그룹은 6자리 초대코드로 합류(`joinByCode`)·공동 read/write. 그룹전환(`openWorkspaceSheet`)에서 '개인 프로필' 또는 그룹 선택(`switchWorkspace`). **가계부·할일은 모드별 최근 컨텍스트를 따로 기억**(`recentWs`)해 토글해도 각자 마지막 사용처로 이어짐. 상세는 [features-ledger.md](features-ledger.md#워크스페이스-개인--그룹-공유).
- **프로필(사진/이름/비밀번호/공개)**: 더보기 프로필 줄 → **내 프로필**(`openProfileSheet`) — 사진 설정/삭제 + 별명 수정 + **계정 이메일(아이디) 표시** + **비밀번호 변경** + **프로필 공개/비공개 토글**(`users/{uid}/profilePublic`; 비공개면 랭킹·비친구에게 **은화+'알뜰'** 익명). 사진은 **256px JPEG base64**(`users/{uid}/photo`). 이름은 내가 속한 모든 워크스페이스 멤버 이름에 반영. **사진 없는 기본 아바타=은화 픽셀 아이콘**(개인, `avatarHtml`), **그룹/가계부 기본=금화**(`wsAvatarHtml`). 멤버 사진은 `loadMemberPhotos`로 캐시.
- **가계부(워크스페이스) 프로필**: 그룹 이름·사진 편집(`openWsProfileSheet`, `wsAvatarHtml`).
- **다크모드**: 토글(localStorage 저장, `applyTheme`/`toggleTheme`).
- **PWA**: 홈 화면 추가 배너(`beforeinstallprompt` → `installApp`), 오프라인 앱 셸 캐시(`sw.js`). 펫캠 dock 토글(`toggleDockHidden`).
- **접근성(A11y)**: `a11yDecorate`(+MutationObserver)가 재렌더 시 role/포커스/라벨을 자동 보강, 전역 키보드 핸들러(Enter/Space·Esc·포커스 트랩).
- **토스트 알림**: 모든 사용자 동작 피드백(`toast`).
- **🔔 푸시 알림(FCM, 서버리스)**: 설정 → 알림 토글로 켜면 **리마인더 푸시**(오늘 미기록 넛지 등)를 받음. 발송은 **GitHub Actions 스케줄 크론**(Blaze 불필요) + FCM. 토큰 `users/{uid}/push`, 클라 `js/push.js`+`firebase-messaging-sw.js`. iOS는 홈화면 설치 PWA(16.4+)에서만 동작. 설정·활성화는 [docs/deploy/push.md](deploy/push.md).
- **온보딩**: 첫 사용자 진입 시 **3스텝 안내 시트**(기록·친구·알뜰홈, `maybeOnboard`/`openOnboarding`, `users/{uid}/onboarded` 1회 멱등, 스킵 가능).

> ✅ 로드맵 기능이 모두 구현되었습니다(가계부 정산·경조사비·대출·권한/공동 설정 + 할일 개인/그룹·공유). 더보기 메뉴에 남은 "예정" 항목 없음.
