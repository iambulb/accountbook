# 변경 이력 (Changelog)

이 프로젝트의 주요 변경 사항을 기록합니다. 형식은 [Keep a Changelog](https://keepachangelog.com/ko/1.1.0/) 를 따르며, 사용자 체감 변경은 모두 여기에 한 줄씩 남깁니다.

분류: `추가`(Added) · `변경`(Changed) · `수정`(Fixed) · `제거`(Removed) · `보안`(Security).

## [Unreleased]

> 새 변경 사항은 여기에 추가하세요. 릴리스할 때 버전 번호와 날짜를 붙여 아래로 내립니다.

### 변경 — 🛒 상점 고양이 크게 + 우리집 고양이 대형 캐러셀 + 슬롯 최대 20(가로 스크롤) + 상단 펫캠 토글
- **상점 고양이 썸네일 확대**: 가구·소비 아이템처럼 큼직하게 — `.shopcard .thumb` 74→86px, 고양이 이미지 56→72px, `.furnfit` 60→74px.
- **우리집 고양이 대형 캐러셀**: 홈의 보유 고양이를 **한 마리씩 화면에 거의 꽉 차게**(카드 폭 80%·이미지 110px) **가로 스와이프**로 넘겨 보게 변경(`.catchips` nowrap+scroll-snap). 탭하면 집에 내보내기/대기, 이름 짓기(연필) 유지.
- **활성 슬롯 최대 20 + 가로 스크롤**: `MAX_SLOTS` 4→**20**. 100금화로 **1칸씩** 확장(`buySlot` +1), 슬롯 행이 오른쪽으로 늘며 **가로 스크롤**(`.slotrow` overflow-x). 끝에 잠금 슬롯(🔒 금화 100).
- **상단바 펫캠 ON/OFF 토글**: 다크모드 버튼 **왼쪽에 캠 아이콘** 추가(`#petcamBtn`) — 탭하면 상단 **펫캠(dock)을 켜고/끔**(`toggleDockHidden`), 꺼짐 상태는 흐리게+대각선 슬래시로 표시(`updatePetcamBtn`). `sw.js` `v3.66.0`.

### 변경 — 🧩 배치 UX(칸 꽉 채움·드래그 배치) + 상점 펫알 크게 + 가계부 전환 사진
- **배치칸에 가구 꽉 차게**: 배치 격자의 가구를 발자국 칸에 꽉 차게 렌더(작게 축소하던 `furnScale` 제거), 팔레트 아이콘도 크게(h:34).
- **팔레트 드래그 배치**: 팔레트 항목을 **그대로 그리드로 드래그**해 배치(`palDown/palMove/palUp`, 따라다니는 고스트·드롭 프리뷰). 드래그 시작 시 남은 수량이 없으면 "없어요" 안내. 탭하면 기존처럼 선택(빈 칸 탭 배치도 유지, 키보드 Enter 선택 지원).
- **상점 펫알 크게**: 이벤트 탭 펫알 썸네일 h56→**66**(달걀 모양 유지, 랜덤박스보다 살짝 크게).
- **가계부 전환 사진**: 전환 목록에 **가계부 프로필 사진**(`wsAvatarHtml`, 없으면 이니셜) 적용, 그룹은 **이름 옆에 멤버 프로필 사진을 작게**(`memberAvatarStack`) 표시. `sw.js` `v3.65.0`.

### 수정/변경 — 📹 캠 세로 확장 + 걷기 깜빡임 수정 + 고양이 더 크게 + 캣타워 중앙에 앉기
- **캠(dock) 세로 확장**: `.cd-room` 70→110px — 고양이가 더 크게·시원하게 보이도록.
- **걷기 시 깜빡임(번쩍번쩍) 수정**: 픽셀 스프라이트를 **서브픽셀 위치**(toFixed(1))로 이동시켜 nearest-neighbor 합성이 매 프레임 흔들리던 것 — **정수 px로 스냅**(`Math.round`, left·bob·lift)해 깜빡임 제거.
- **고양이 더 크게 + 픽셀 또렷하게**: dock 40→**48px(=원본 48px 1배, 픽셀 완벽)**, 홈 방 58→**64px**. 걷는 간격도 함께 조정.
- **캣타워 중앙에 앉기**: 좌측하단 앵커라 상호작용 x가 가구 오른쪽으로 밀려 고양이가 **허공에 뜨던** 문제 — 가구 그래픽 중앙(`좌측edge + fh*aspect/2`, `FURN_ASPECT`)을 계산해 **고양이 중심이 가구 정중앙**에 오도록(도착 판정도 고양이 중심 기준). 캣타워·방석은 정중앙에 앉음.

### 변경 — 📷 더보기 "알뜰샵 dock" → "펫캠"(캠 아이콘) + 바텀시트 아래로 드래그해 닫기
- 더보기 설정의 dock 토글 라벨을 **"알뜰샵 dock" → "펫캠"** 으로 변경하고, 아이콘을 고양이 얼굴 → **단순 라인 캠 아이콘**(`MORE_ICON.cam`, 기존 라인 아이콘 스타일과 통일)으로 교체.
- **바텀시트 스와이프 다운 닫기**: 시트(설정·정산·경조사비·지출입력·알뜰샵 등) 상단 **핸들/헤더를 아래로 드래그**하면 시트가 따라 내려오고, 충분히(≈150px) 내리면 닫히며, 살짝이면 제자리로 복귀(`setupSheetDrag`, 포인터 이벤트). 드래그에 맞춰 오버레이도 함께 흐려짐. X 버튼 닫기는 그대로. `sw.js` `v3.63.0`.
- **캠·홈 방 고양이 크기 확대**: 너무 작아 픽셀아트 느낌이 약했던 걸 키움 — 홈 방 46→58px, dock 40→... 실제 걷는 고양이 표시 크기를 키워 `image-rendering:pixelated`의 도트가 더 또렷하게 보이도록(간격도 함께 조정). (홈 `hh` 46→58, dock `hh` 34→40)
- **`showmethemoney` 코드 중복 사용 허용**: 일반 프로모 코드는 사용자당 1회 유지, **`showmethemoney`만 여러 번 사용 가능**(`REUSABLE_CODES`, 재사용 코드는 `codes[key].n` 사용 횟수만 기록하고 매번 지급).

### 변경 — 🏠 홈 방 세로 확대 + 큰 가구 원근 강화 + 금·은화 간격
- **홈 방 세로 확대**: 커진 캣타워가 잘리던 문제 — `.catroom` 높이 200→244px, 바닥 영역을 낮춰(`.cr-floor`/`.cr-base` 58→54%) **벽지(윗부분)를 소폭 세로 연장**(42→46%). 가구 세로 배치 폭도 방 비율에 맞춰 소폭 조정.
- **큰 가구는 멀수록 더 작게(원근 강화)**: `furnRoomH` 원거리 축소폭을 가구 크기에 비례시킴 — 방석·그릇 같은 작은 가구는 멀어도 완만하게, **캣타워처럼 큰 가구는 멀리 있을수록 더 뚜렷하게 작아지도록**(`shrink = base + (mult-1)·k`).
- **알뜰샵 금화·은화 간격**: 코인바 두 배지가 붙어 보이던 것 — `.coinbar`에 `gap:10px` 추가로 사이를 벌림. `sw.js` `v3.61.0`.

### 변경 — 🐈 삼색 → 토터셸 교체 + 정면-옆이동 방지 규칙 명문화
- **삼색(`cat_calico`) 스프라이트 교체**: `chibi_pixel_art_tortoiseshell_cat_mixed_black_and.zip`(코숏·토터셸, `Walk/east` 옆걷기)로 `assets/pets/cat_calico/` walk.png+정지 4방향 재생성. id·등급·가격 그대로(보유/뽑기 영향 없음), 카탈로그 설명·품종(코숏) 갱신.
- **정면으로 옆이동 방지 점검·규칙화**: 전 고양이 `walk.png` 방향을 검증 — **`cat_white`만 정면(south) 걷기**이며 이미 `frontWalk:true`로 이동 중엔 east 정지스틸(옆 보기)을 쓰도록 처리됨(정면 슬라이딩 없음). 나머지는 모두 east 옆걷기. 재발 방지를 위해 **CLAUDE.md에 "걷기 시트는 반드시 east, 없으면 `frontWalk:true` 필수" 규칙 추가**. `sw.js` `v3.61.0`.
- **한정 등급 이름을 무지개색**: 상점 카드·보유 칩의 이름을 `catNameSpan`으로 렌더 — **한정은 `.tier-limited`(무지개 그라데이션 애니메이션)**, 나머지는 등급 색(일반=기본 잉크).
- **가구 앵커를 "배치칸 좌측하단"으로**: 방 렌더에서 가구를 가로 중앙정렬하던 것을 **좌측 edge 기준**(`left`=칸 좌측, CSS `translateX(0)`·`transform-origin:left bottom`)으로, 바닥은 **발자국 앞줄(front row) 기준**(`p.r+footH-1`)으로 맞춰 멀티셀 가구가 위로 떠 보이던 문제 해결. 상호작용 좌표(`buildActors`)도 동일 매핑으로 일치.
- **원근 완화**: 멀리 있는 가구가 너무 작아지지 않도록 축소폭을 줄이고 가까운 가구는 더 크게(`furnRoomH` unit: 홈 `16-depth*3`·dock `11-depth*2`).
- **캣타워·스크래처 실물감 있게 크게 + 상호작용**: 방 렌더 높이를 `ROOM_H` 배율로(캣타워 6.2·스크래처 2.9·화장실 1.5·방석 1·그릇 0.8) 재정의해 캣타워는 크게·스크래처는 고양이 키만큼. 가구로 다가가는 빈도(`seek`)를 높여 **캣타워(3층 올라가 쉼)·스크래처(옆에서 머무름) 상호작용이 더 자주** 보이도록. `sw.js` `v3.59.0`.

### 추가 — 🐈 고양이 3종(벵갈·폴드·보라) + 고양이별 이름 짓기(등급색 유지)
- **고양이 3종 추가**: 벵갈(`cat_bengal`, 벵갈·골든 반점, 고급)·폴드(`cat_fold`, 스코티시폴드·접힌 귀, 희귀)·보라(`cat_bora`, 코숏·오드아이, 특별). 각 zip(`Walk/east`+8방향 rotations)을 처리해 `assets/pets/<id>/` walk.png(288×48)+정지 4방향 생성, `PET_CATALOG`·`PET_SPRITES`·`CAT_TIER`(→`TIER_PRICE`로 가격 자동)·`sw.js` APP_SHELL 등록. **이제 고양이 11종.**
- **고양이별 이름 짓기**: 홈 "우리집 고양이" 칩의 **✎ 버튼**으로 이름 변경(`openRenameCat`, 최대 12자, `owned.cats[id].name`에 저장, `catName`이 커스텀 이름 우선). 빈 값이면 기본 이름으로 복원.
- **이름은 등급 색 유지**: 커스텀 이름도 `catNameSpan`으로 **등급 색**(한정=무지개)으로 표기 — 홈 칩·뽑기 결과 등장 이름에 적용. `sw.js` `v3.58.0`.

### 변경 — 🏷️ 상점 품종 라벨(샴) + 동물 이름을 등급 색으로 표기
- **품종 라벨 정확화**: 상점 고양이 카드의 품종 태그가 전부 "코숏"으로 고정돼 있던 것을 `PET_CATALOG.breed`(없으면 기본 "코숏")로 표기 → **샴은 "샴"** 으로 표시.
- **이름을 등급 색으로**: 상점 카드·보유 칩의 고양이 이름을 **등급 색**으로 칠함(`catTierColor`: 고급=초록·희귀=파랑·특별=보라·전설=금색·한정=핑크). 일반 등급은 흰색이라 밝은 배경에서 안 보이므로 기본 잉크색(`var(--text)`)으로 처리.

### 변경 — 📏 캠(dock) 세로 축소 + 배치 앞뒤 반전 + 프리뷰 설명 제거
- **상단 캠(dock) 높이 축소**: `.cd-room` 96px→**70px**, 바닥 비중 52%→**66%** — 위쪽 벽지가 휑하던 부분을 줄여 컴팩트하게.
- **배치 격자 앞뒤 반전**: 격자 **윗줄=방 뒤(멀리·작게·위)**, **아랫줄=방 앞(가까이·크게·아래)** 로 매핑 반전(`propMarkup`/`buildActors` `depth=(12-r)/11`, 그리기 순서도 앞이 위로 오도록 정렬 반전) — 위로 옮기면 캠에서 뒤로, 앞쪽에 놓으면 앞으로 보임.
- **배치 탭 미리보기 아래 안내문 제거**(방향·사용법 힌트라인 삭제, 미니 프리뷰는 유지). `sw.js` `v3.57.0`.

### 추가/변경 — 🐈 샴 추가 + 등급 재배치 + 걷기/정지 모션 개선
- **샴(`cat_siamese`) 고양이 추가**: `hibi_pixel_art_siamese_cat_cream_body_with_dark_fa.zip`(`Walk/east` 옆걷기 + 8방향)을 처리해 `assets/pets/cat_siamese/` 생성, `PET_CATALOG`/`PET_SPRITES` 등록. 등급 **한정**. → 고양이 8종.
- **등급 재배치**: 턱시도 한정→**전설**, 하양 전설→**특별**, 샴=**한정**. `TIER_PRICE`로 가격 자동 반영(하양 800→400, 턱시도 1500→800, 샴 1500).
- **이동=옆 걷기만, 정지·가구쉼=정면(south)**: 이전엔 쉴 때 4방향(앞/뒤/좌/우)을 무작위로 봐서 "정면 이미지로 옆 이동"이 자주 보였음 → **옆으로 움직일 땐 옆 걷기 시트만**, **가만히 서 있거나 캣타워·방석에서 쉴 땐 정면(south)** 만 보도록 정리(`enterPose`/`furnSpot` south 고정). **정면으로 서 있는 시간도 늘림**(`poseDur` 상향).
- **걸음 자연스럽게(무빙워크 방지)**: 걷기 애니메이션 주기를 이동 속도에 맞춰 동적으로(`--wdur`/`walkDur`, 발이 바닥을 미끄러지지 않게) + 속도 폭을 살짝 좁힘. 삼색 등 옆걷기가 어색해 보이던 문제 완화.
- **하양(`frontWalk`) 처리**: 하양은 export에 옆(east) 걷기가 없어 walk.png가 정면이라, **이동 중엔 east 정지스틸로 옆을 보게** 하여 정면으로 옆 이동하던 현상 제거(진짜 옆걷기 애니메이션은 east 걷기 export가 오면 교체). `sw.js` `v3.55.0`.

### 변경 — 🎥 방을 위에서 내려다보는 뷰로 + 배치 미니 프리뷰 + LIVE 배지 좌상단
- **위에서 아래로 내려다보는 웹캠 뷰**: 방(홈·dock)의 **바닥(회색)을 넓히고 벽지를 좁힘**(홈 바닥 38%→58%, dock 34%→52%, 벽 그라데이션 62%→42%) — 내려다보는 느낌, 벽지 덜 보임.
- **배치 처음/끝을 바닥에 실제 반영**: 가구 깊이 위치를 px→**방 높이 %**로 바꿔(`propMarkup`, 앞 행 3%→뒤 행 53%) 배치 **맨 앞 행=바닥 앞끝, 맨 뒤 행=바닥 뒤끝(벽 경계)** 까지 실제로 펼쳐지도록 함. 원근 비율도 강화(뒤로 갈수록 더 작게, `furnRoomH` 단위 상향).
- **배치 탭 미니 웹캠 프리뷰**: 배치 격자 위에 **현재 배치를 실제 방 모습으로 보여주는 미니 프리뷰**(`.miniroom`, 표시 전용) + "격자 윗줄=방 앞/아랫줄=방 뒤" 안내 — 방향 헷갈림 방지.
- **LIVE 배지 좌측 상단으로 이동**(`.cr-cam`, 하단→top-left). `sw.js` `v3.55.0`.

### 추가/수정 — 🧹 일괄 돌보기 버튼 + 그릇 탭 버그 수정 + 그릇/화장실 배치 제한
- **그릇 탭이 안 먹던 버그 수정**: 홈·캠 방에서 고양이 무대(`.cr-stage`)가 그릇/똥 위를 덮어 클릭을 가로채던 문제 — `.cr-stage`에 `pointer-events:none`을 주어 클릭이 그릇/똥으로 통과되게 함. 캠(dock)의 그릇/똥도 개별 탭으로 급여·수거 가능(빈 곳 탭은 기존대로 알뜰샵 열기).
- **일괄 돌보기 버튼**(홈·캠 우측 상단, `cr-batch`/`batchCare`): 한 번에 **모든 빈 그릇을 사료·물로 채우고**(보유분 소모), **쌓인 똥을 모두 치워** 은화를 획득. 획득 은화가 **은화 카운터로 날아 들어가는 연출**(`coinFlyFx`, 카운터 톡 튀기 `bump`) + 카운트 증가.
- **밥·물그릇·화장실 배치 제한**: `CARE_ITEMS`는 **고양이 최대 마릿수(`slotCount`)만큼만** 배치 가능(초과 시 안내). 방·캠 렌더를 공용 헬퍼(`propMarkup`/`distributePoops`)로 통합해 캠에서도 채움·똥이 동일하게 표시.
- **개발자 다마고치 테스트(canel94 전용)**: 개발자 패널에 "다마고치 테스트(즉시)" 섹션 — **사료·물 +10 / 그릇 다 채우기 / 그릇 만료→똥 / 똥 +3 / 은화 +100**(`devGiveConsum`/`devFillAll`/`devExpireBowls`/`devAddPoop`/`devAddCoins`). 3시간 안 기다리고 급여·배변·수거 흐름을 즉시 검증. `sw.js` `v3.54.0`.

### 추가 — 🐾 활성 슬롯 확장(금화 100) + 등급별 가격 재산정 + 캠 하단 이동
- **4번째 활성 슬롯을 금화 100으로 확장**: 집에 내보낼 수 있는 슬롯이 기본 3칸인데, 홈 상단에 **활성 슬롯 행**을 추가하고 오른쪽 끝에 **잠금 슬롯(🔒 + 금화 100 표기)** 을 둠. 탭하면 **금화 100**으로 4번째 슬롯을 영구 확장(`home.slots` 3→4, `BASE_SLOTS`/`MAX_SLOTS`/`SLOT_PRICE`, `slotCount()`/`buySlot()` 원자 트랜잭션 — 금화의 첫 소비처). 내보내기 상한·dock/방 렌더 slice가 모두 `slotCount()`를 따름.
- **고양이 상점 가격을 등급·확률에 맞춰 재산정**: 등급이 오를수록 약 2배씩(일반50·고급100·희귀200·특별400·전설800·한정1500). `TIER_PRICE`를 두고 `CAT_TIER`를 단일 소스로 `PET_CATALOG.price`를 자동 산정(새 고양이도 등급만 지정하면 가격 자동). 알(펫알 100은화·오픈 시 금화+1·중복 30은화 환급)을 감안해 **흔한 등급은 알보다 싸게·희귀 등급은 비싸게** → "직접 구매 vs 뽑기" 선택지 성립.
- **웹캠 방 LIVE 캠 배지를 상단→하단으로**: 아래에서 촬영하는 컨셉으로 `.cr-cam`을 방 하단에 배치. `sw.js` `v3.54.0`.

### 추가 — 🍚 다마고치: 사료·물 소비 + 급여/배변 루프 + 화장실·물그릇 + 은화 벌기
- **소비 아이템(사료·물)**: 상점에 **소비 탭** 신설, `CONSUM_CATALOG`(사료·물, 각 **1 은화**, **배치 불가**) 구매(`buyConsum`) — `game.consum:{food,water}`에 수량 저장. 아이콘 `M_FOOD`/`M_WATER`.
- **물그릇·펫 화장실 가구 추가**: `ITEM_CATALOG`에 `waterbowl`(1×1)·`litterbox`(2×2). 밥그릇 기본 이미지에서 **밥(F) 제거**(빈 그릇 `M_BOWL`), 채움 변형 `M_BOWL_FOOD`/`M_WATERBOWL_WATER` 신설.
- **급여(홈 탭)**: 홈 화면에서 **밥그릇/물그릇을 탭**(`feedBowl`)하면 사료/물 1 소모 후 `placed[key].filledAt=now`로 채움 — 이미지도 밥/물이 채워짐(`furnRoomSvg`가 채움 상태 반영, `isFilled` 3시간 이내).
- **배변 정산(다마고치)**: 채운 지 **3시간 뒤 그릇이 비워지고**(`reconcilePets`, 멱등), **비운 그릇 하나당 `home.poops`+1** → 화장실 위에 작은 똥(`M_POOP`)으로 표시(화장실당 최대 5개, 라운드로빈 분배). 앱 켜둔 동안 60초마다·스냅샷·홈 렌더 시 정산.
- **똥 수거 = 은화**: 똥을 탭하면 **+2 은화**(`collectPoop`, `POOP_REWARD`) + 작은 획득 연출(`.poopfx` 떠오르는 +2). 가끔 앱에 들어와 상호작용해 은화를 버는 컨셉. `sw.js` `v3.53.0`.

### 수정 — 🐛 스프라이트 고양이가 dock·방·상점 어디서도 안 보이던 문제(실제 원인)
- **`--sheet`/`--idle` 상대경로 → 절대 URL로 고정**: `catActorHTML`가 `--sheet:url(assets/pets/…)`를 **상대경로**로 넣으면, `styles.css`의 `.cspr{background-image:var(--sheet)}`가 그 URL을 **스타일시트 위치(`/css/`) 기준**으로 해석해 `/css/assets/pets/…`를 요청 → **404 → 배경 없음 → 고양이 전부 안 보임**(dock·홈 방 공통). `assetUrl(p)=new URL(p, document.baseURI).href`로 **문서 기준 절대 URL**을 만들어 `--sheet`·`--idle`(및 `sprStill`)에 사용하도록 수정. 크롬 헤드리스로 재현·검증(수정 전 `/css/assets/…` 404 → 수정 후 `/assets/…` 200). *직전 "새 SW 자동 새로고침"은 유효한 개선이지만 이 버그의 원인은 캐시가 아니라 URL 해석이었음.* `sw.js` `v3.52.0`.

### 변경 — 🐈 방·dock 가구 크기를 발자국(칸)에 비례해 키움(상호작용용)
- **홈·캠(dock) 방의 가구 렌더 크기를 발자국 세로 칸수에 비례**: 기존엔 캣타워·스크래처가 표시배율(`furnScale`)이 같아 방에서 **같은 높이**로 보였는데, 이제 `furnRoomH(id,isDock,depth)`가 **footH 칸수에 비례**(캣타워 6 > 스크래처 2 > 방석·밥그릇 1)해 렌더 → 캣타워가 확실히 제일 크고 고양이보다 커서 **3층 상호작용**이 자연스럽게 맞음(홈 캣타워 ~72px, 고양이 46px). 밥그릇<방석 유지, 뒤(depth)로 갈수록 작게.
- **상호작용 높이 일치**: 걷기 엔진의 가구 높이(`fh`)를 렌더와 **동일한 `furnRoomH`**로 계산 → 캣타워 3층 발판 `lift`(≈0.30/0.62/0.92 높이)가 실제 렌더 높이에 맞아떨어짐. `sw.js` `v3.51.0`.

### 추가 — 🌀 카오스(cat_chaos) 고양이 추가
- **카오스 고양이 추가**: `imple_pixel_art_chaos_cat_dark_grey_and_brown_swir.zip`(`Walk/east` 옆걷기 + 8방향 rotations)을 처리해 `public/assets/pets/cat_chaos/`에 `walk.png`(288×48) + 정지 4방향 생성. `PET_CATALOG`에 `cat_chaos`(종=cat, 이름=카오스, 85 은화) + `PET_SPRITES`·`CAT_TIER`(전설 등급) 등록 → dock·방·상점·보유칩·뽑기 어디서나 스프라이트로 표시. **이제 고양이 7종**(고등어·치즈·삼색·하양·턱시도·까망·카오스). `sw.js` `v3.50.0`.

### 변경 — 🐈 캣타워 3×6·3층 상호작용 + 가구별 머무름 + 알 상하 반전
- **캣타워 3×6칸(세로형)·3층 아트**: `M_TOWER`를 발판 3개가 뚜렷한 세로 타워(11×22 ≈ 3:6)로 다시 그리고, 발자국을 **가로3×세로6**으로 지정. 발자국을 사각형으로 일반화(`footW`/`footH`, `itemFoot`→`{w,h}`) — 배치 겹침판정(`areaFree`)·격자 렌더(`.gitem` w/h %)·방 중앙정렬·드롭 프리뷰 모두 반영.
- **가구별 상호작용(랜덤 시간 머무름)**: 걷던 고양이가 가구에 다가가 자리 잡음(`furnSpot`/`enterInteract`) — **캣타워=3층 중 한 층에 올라가 정면 보며 쉼**(`lift`로 발판 높이 올림), **방석=위에 잠시 올라감**, **밥그릇=뒤에서 앉아 먹음**, **스크래처=옆에서 잠시 머무름**. 각 2~6초 랜덤. 액터에 가구 렌더높이(`fh`)·올라감(`lift`) 추가, 재출발 시 원위치.
- **상점 가구 썸네일 크기 정규화**: 실제 크기(캣타워 큼)와 무관하게 **60px 상자에 비율 맞춰**(`.furnfit`) 적당히 표시 — 큰 캣타워가 카드를 넘치지 않음.
- **알 이미지 상하 반전**: 위가 둥글던 것을 뒤집어 **위는 좁고(뾰족) 아래가 넓고 둥근** 실제 달걀 형태로(`M_EGG`/균열1·2). 무지개 물음표는 위 방향 그대로 유지. `sw.js` `v3.49.0`.

### 수정 — 🔄 새 서비스워커 활성 시 자동 새로고침(구 캐시로 고양이 안 보이던 문제)
- **에셋/코드 갱신이 즉시 반영되도록 SW 업데이트 시 1회 자동 새로고침**: 서비스워커가 stale-while-revalidate라 이전 캐시의 `cats.js`(구 `assets/cats/*` 경로)를 계속 물어 **dock/방에 스프라이트 고양이가 404로 안 보이던** 현상 방지. `main.js`에서 `navigator.serviceWorker`의 `controllerchange`를 감지해, **이전 controller가 있던 경우에만**(즉 최초 설치 제외) `location.reload()`로 새 코드/에셋을 즉시 적용(`skipWaiting`+`clients.claim`과 짝). *dock 렌더 로직 자체는 jsdom 재현으로 정상 확인 — 원인은 스프라이트 경로 이전 후의 구 캐시.*

### 추가 — 🐈‍⬛ 까망(cat_black) 고양이 SVG → PNG 스프라이트 교체
- **까망 고양이를 PixelLab 스프라이트로 교체**: `simple_pixel_art_cat_black_yellow_eyes_extra_round.zip`(`Walk/east` 옆걷기 + 4방향 rotations)을 처리해 `public/assets/pets/cat_black/`에 `walk.png`(288×48) + 정지 4방향 생성. `PET_SPRITES`에 `cat_black` 등록 → 마지막 SVG 고양이였던 까망까지 스프라이트로 전환. **이제 고양이 6종 전부 PNG 스프라이트**(고등어·치즈·삼색·하양·턱시도·까망). 카탈로그·가격·등급(epic) 변경 없음. `sw.js` `v3.48.0`.

### 추가 — 🧩 배치 에디터: 2×2 멀티셀 + 드래그 이동 + 탭 회수/판매
- **캣타워·스크래처 2×2칸 점유**: `ITEM_CATALOG.foot`(1 또는 2)로 배치 발자국을 정의(`itemFoot`). 배치·이동 시 발자국 영역이 격자를 벗어나거나 다른 가구와 **겹치면 차단**(`areaFree`/`occupiedCells`). 저장 형식은 그대로 `placed["r_c"]={itemId}`(좌상단 칸 기준, 발자국은 id에서 파생 — 하위호환 유지).
- **배치 격자 재구성**: 12×12 CSS 그리드 셀 나열 → **배경 라인 격자 + 가구를 절대좌표(%)로 오버레이**(`.gitem`). 빈 칸 탭 시 좌표→칸 계산으로 배치(`placeClick`/`cellFromPoint`), 발자국이 넘치지 않게 좌상단 보정.
- **드래그 이동**: 놓인 가구를 포인터로 끌어 이동(`giDown/giMove/giUp`, 포인터 캡처). 이동 중 목표 영역을 초록/빨강 프리뷰(`.gdrop`)로 표시하고, 유효하면 `home/placed` 멀티경로 업데이트로 커밋. 이동/탭 구분은 6px 임계값.
- **탭 → 회수/판매 메뉴**: 배치된 가구를 탭하면 팝업(`openItemMenu`) — **회수**(인벤토리로 되돌림, 보유 유지) / **판매**(보유에서 제거 + **10 은화**, `ITEM_SELL`, `sellPlaced` 원자 트랜잭션) / 닫기. 방(dock·홈)에선 2×2 가구가 발자국 중앙에 오도록 x 보정. `sw.js` `v3.48.0`.

### 변경 — 🪑 가구 표시 크기 차등(캣타워·스크래처↑, 방석·밥그릇↓)
- **가구별 표시 배율(`ITEM_CATALOG.size`) 도입·적용**: 지금까지 미사용이던 `size` 필드를 **표시 배율**로 활용하는 `furnScale(id)`를 만들어 상점 썸네일·배치 팔레트·방(dock·홈)·배치 격자에 일관 반영. **캣타워·스크래처는 2배(2x2)로 크게**, **방석(0.6)·밥그릇(0.45)은 지금보다 작게**(밥그릇이 방석보다 조금 더 작음). 배치 격자에선 `.gsc` 래퍼에 `scale(size/2)`로 상대 크기를 표시(칸 넘침 없이). 배치 단위는 그대로 1칸(격자 점유 규칙 변경 아님). `sw.js` `v3.47.0`.

### 변경 — 🥚 알 더 둥글게 + 오픈 빛 연출을 은은한 픽셀 감성으로
- **펫알(알) 실루엣을 더 둥글게**: 뾰족하던 윗부분을 완만하게 다듬어 위·아래가 고르게 둥근 달걀 형태로 `M_EGG`(및 균열1·2) 재작성(무지개 물음표·우측 그림자 유지). 상점 썸네일·뽑기 연출에 공통 반영.
- **알·박스 오픈 빛을 "은은한 도트 픽셀" 감성으로**: 열 때 매끈한 원형 흰 강광(쨍)이 나던 것을 정리 — ① 누광(`fx-leak`)을 흰 코어·후광(box-shadow) 대신 **등급색 계단식(블록) 링**으로 바꾸고 밝기↓(peak opacity 0.95→0.6), ② 폭광(`fx-flash`)을 부드러운 소형 블룸으로 축소, ③ 회전 광선(`fx-rays`)은 `steps()` 회전+투명도↓로 각지고 은은하게, ④ 튀는 빛 입자(`fx-particle`)를 둥근 점 대신 **각진 픽셀 조각**으로, ⑤ 한정 무지개 링도 6색 하드 스톱+`steps()` 회전으로 도트 느낌. 등급별 밝기 배율(`--lk`)은 유지. `sw.js` `v3.45.0`.

### 변경 — 🐱 삼색(cat_calico) 고양이 SVG → PNG 스프라이트 교체
- **삼색 고양이를 PixelLab 스프라이트로 교체**: `simple_pixel_art_calico_cat_white_with_orange_three.zip`(8방향 rotations + `Walk/east` 옆걷기)을 처리해 `public/assets/pets/cat_calico/`에 `walk.png`(288×48, 6프레임) + 정지 4방향 생성. `PET_SPRITES`에 `cat_calico` 등록 → 기존 SVG 매트릭스 삼색을 스프라이트로 대체(정상 옆보기 걷기 + 4방향 쉬기). 이제 스프라이트 고양이는 **고등어·치즈·삼색·하양·턱시도 5종**, SVG는 까망만 남음. 카탈로그·가격·등급(rare) 변경 없음. `sw.js` `v3.44.0`.

### 추가 — 🤵 턱시도(cat_tuxedo) 고양이 스프라이트
- **턱시도 고양이 추가**: `simple_pixel_art_tuxedo_cat_black_body_with_white.zip`을 처리해 `public/assets/pets/cat_tuxedo/`에 `walk.png`(288×48, 6프레임) + 정지 4방향 생성. 이 zip은 8방향 rotations와 **옆보기(east) 걷기**(`Walk-<hash>/east`)를 포함해 **정상적인 옆걷기 시트**로 구성(하양과 달리 정면 걷기 아님). `PET_CATALOG`에 `cat_tuxedo`(종=cat, 이름=턱시도, 75 은화) + `PET_SPRITES`·`CAT_TIER`(한정 등급) 등록 → dock·방·상점·보유칩·뽑기 어디서나 스프라이트로 표시. `sw.js` `v3.43.0`(APP_SHELL에 `cat_tuxedo/` walk+4방향 추가). *기존 SVG 까망(`cat_black`)과는 별개의 고양이.*

### 변경 — 🐾 고양이 전용 → "모든 네발 동물" 에셋 파이프라인 일반화
- **경로 `assets/cats/` → `assets/pets/<id>/`**: 동물 에셋을 종 구분되는 폴더로 이관. id를 **`<species>_<색>`** 로 변경(`mackerel`→`cat_mackerel`, `cheese`→`cat_cheese` 등, 예정 강아지=`dog_corgi`). 원본 zip은 `assets/pets/_zips/`.
- **변수 일반화**: `CAT_CATALOG`→**`PET_CATALOG`**, `CAT_SPRITES`→**`PET_SPRITES`**. `PET_CATALOG` 각 항목에 **`species`**(cat/dog/rabbit…) 필드 추가(향후 분류·필터용). `CAT_PALS`·`CAT_TIER` 키도 새 id로 정렬.
- **id 하위호환 마이그레이션**: `id`는 RTDB 저장 키라, 기존 사용자의 보유(`owned.cats`)·활성(`home.active`)에 남은 구 id를 `PET_ID_MIGRATE`(구→신)로 `normalizeGame` 읽기 시 자동 이관(다음 쓰기에 영구 반영) — **보유 고양이 유실 없음**. 개발자 뽑기 설정도 알려진 id만 반영하도록 `effCatTier` 가드.
- **문서 일반화**: `docs/cat-asset-pipeline.md` → **`docs/pet-asset-pipeline.md`**("Pet Asset Pipeline")로 개칭·일반화(zip 규격/시트 합치기/4방향/상태머신 로직은 동일 유지, Walk 방향이 east가 아닌 경우 주의 추가), `CLAUDE.md` 등록 문구를 "동물 에셋"으로 갱신. zip 규격/상태머신 로직은 동물 무관 동일.

### 추가 — 🐈‍⬛ 하양(파란 눈) 고양이 PNG 스프라이트
- **하양(`cat_white`) 고양이를 PixelLab 스프라이트로 추가**: `chibi_pixel_art_white_cat_pale_blue_eyes_sleepy_fa.zip`을 처리해 `public/assets/pets/cat_white/`에 `walk.png`(288×48, 6프레임) + 정지 4방향(south/north/east/west) 생성. `PET_SPRITES`에 `cat_white` 등록 → dock·방·상점·보유칩·뽑기 결과 어디서나 스프라이트로 표시(기존 SVG 하양 대체). export에 **옆보기(east) 걷기가 없어 정면(south) 걷기 6프레임**으로 시트를 구성(걸을 때 정면, 쉴 때 4방향 정지). `sw.js` `v3.42.0`(APP_SHELL에 `cat_white/` walk+4방향 추가; 이전 `assets/cats/*` 잘못된 경로도 `assets/pets/cat_*`로 정정).

### 변경 — 🥚 펫알·랜덤박스 도트 리디자인 + 원근 수정 + 껍질 깨짐 연출 강화
- **펫알(알) 도트 재디자인**: 위가 좁고 아래가 둥근 **달걀 실루엣**으로 `M_EGG`(및 균열1·2)를 다시 그림. 중앙에 **크고 두꺼운 무지개(R→O→Y→G→B→P) 물음표**를 새로 넣고, 우측에 은은한 그림자(S)로 볼륨감. 균열 단계(`M_EGG_C1`/`C2`)는 위쪽부터 잔금이 번지도록 재작성.
- **랜덤박스 도트 재디자인**: 뚜껑(윗면 `C`+앞면 `L`)과 몸체(`W`)로 **뚜껑/밑부분을 또렷하게** 정리. 상자에 그려져 있던 **고양이 얼굴을 제거**하고, 앞면 중앙에 **알과 같은 무지개 물음표**를 넣음(`M_BOX`·`BOX_PAL` 재작성 — 기존 리본/보 `R/K/D/M` 제거).
- **상단 웹캠 방 기구물 원근 수정**: 배치 가구가 **뒤(행이 클수록)일수록 작게·위로, 앞일수록 크게·아래로** 보이도록 크기 계산을 반전(`renderDockProps`·`catHomeHtml`). 앞 가구가 뒤 가구를 가리도록 **뒤→앞 순서로 그림**(깊이 정렬). 걷는 고양이의 **정지·뒤돌아보기(4방향 쉬기)** 가 더 자주 보이도록 유휴 빈도 상향.
- **알 껍질 깨짐 연출 리얼화**: 절정에서 **껍질 조각이 좌우로 튀어나가 아래·옆에 흩어져 앉는** 연출 추가(`fxShells`·`.fx-shell`, 알 전용, 큰 조각 2 + 잔조각). 알은 조각이 자리 잡을 시간을 조금 더 줌.
- **깨질 때 새어나오는 빛을 등급에 맞춰 연출**: 껍질 사이 누광(`fx-leak`)을 **등급색 + 흰 코어**로 바꾸고, **등급이 높을수록 빛이 크고 밝게**(`--lk` 배율, 후광 `box-shadow`)—한정은 기존 무지개 유지. `sw.js` `v3.40.0`.

### 추가 — 🐈 고양이 4방향 쉬기 + 치즈 스프라이트 + zip 파이프라인 규칙화
- **걷다 멈춰서 앞/뒤/옆 보며 쉬기(정지 4방향)**: 스프라이트 고양이가 유휴 상태(`enterPose`)에 들어가면 이전엔 단일 `idle.png` 한 장만 보였는데, 이제 PixelLab `rotations`의 **south(앞)·north(뒤)·east(우)·west(좌) 4방향 정지 이미지 중 하나를 무작위**로 보여줌(`CAT_SPRITES.stills`, `--idle` 동적 지정). 정지 이미지는 이미 올바른 방향이라 **플립하지 않음**(`scaleX(1)`). `reduced-motion`에서는 처음부터 south(앞) 정지로 고정.
- **치즈(cheese) 고양이 PNG 스프라이트 걷기 추가**: PixelLab zip의 `Walk/east` 6프레임을 이어 붙인 `walk.png`(288×48) + 4방향 정지로 치즈도 고등어처럼 CSS `steps(6)` 걷기·4방향 쉬기 지원(`CAT_SPRITES.cheese`). 고등어(mackerel)도 4방향 정지를 새로 받아 `idle.png` 단일 장 → 4방향으로 소급 교체.
- **PixelLab zip 자동처리 파이프라인 문서화**: `public/assets/cats/_zips/`에 zip을 넣으면 따르는 규칙을 [docs/cat-asset-pipeline.md](cat-asset-pipeline.md)로 정리하고 `CLAUDE.md`에 등록. 원본 zip은 `_zips/`에 보관하며 APP_SHELL 캐시엔 넣지 않음. `sw.js` `v3.39.0`(치즈 walk+4방향·고등어 4방향 추가, `idle.png` 제거).
- **스프라이트 고양이 아트를 모든 표시 위치에 일관 적용**: 걷는 무대(dock·방)뿐 아니라 **상점 카드·보유 고양이 칩·뽑기(펫알) 결과 썸네일**도 스프라이트 고양이는 `south.png`(정면 PNG)로 보이도록 단일 진입 함수 `catFace(id,{h})` 신설(`hasSprite`로 PNG/SVG 자동 분기, `.catpx` `<img>`). 이전엔 이 세 곳이 항상 SVG 정면만 그려 치즈·고등어도 옛 도트로 보였음. 파이프라인 문서에 "모든 표시 위치에 일관 적용(catActorHTML/catFace만 사용)" 규칙 추가.

### 변경 — 🏷️ "고양이집"→"알뜰샵" 개칭 + 은화 아이콘 둥글게 + 앱 대표 아이콘 교체
- **게임화 기능 이름 `고양이집` → `알뜰샵`**: 시트 제목(`openSheet`), 더보기 그리드 셀, 설정의 `dock` 항목 라벨을 모두 `알뜰샵`으로 변경(기능 동작 동일).
- **은화(코인) 아이콘 오른쪽을 둥글게**: `M_COIN` 외곽선이 왼쪽만 둥글고 오른쪽은 세로로 평평하던 문제 — 중심(5.5) 기준 **좌우 대칭 원형**으로 매트릭스 재작성(양쪽 모두 둥글게), 고양이 얼굴(눈·코)도 중앙 정렬. 정적 `icons/coin.svg`(로그인/인앱)도 동일 매트릭스로 재생성.
- **은화 아이콘을 앱 대표 아이콘으로 교체 + 이전 아이콘 제거**: 기존 앱 아이콘(`icons/icon.svg`·`icon-192/512`·`maskable-512`)을 **둥근 은화 코인**(연한 그라데이션 배경 라운드 타일)으로 전면 교체 — 이전 아이콘 디자인 삭제. PNG는 변환 도구 없이 **순수 Node PNG 인코더**(`zlib`)로 픽셀아트를 직접 래스터화해 생성(192·512·maskable 512, 코인 셀 정수배로 크리스프). `sw.js` `v3.37.0`.

### 변경 — 🐱 고등어 고양이 PNG 스프라이트 걷기(PixelLab 시트)
- **고등어(mackerel) 고양이를 PNG 스프라이트 시트 기반 걷기로 교체**: `design_sample/files/`의 PixelLab 에셋(`cat_mackerel_walk.png` 288×48 6프레임 east + `cat_mackerel_idle.png` 48×48)을 `public/assets/cats/mackerel/`에 넣고, dock·방 무대에서 고등어는 **CSS `steps(6)` 스프라이트 애니메이션**(`.cspr`)으로 걷도록 함(md 지시서 방식). 좌우 이동·방향 전환(`scaleX`)·유휴/가구 상호작용은 기존 통합 rAF 엔진 유지, 정지·포즈 시 `idle.png`로 전환(`.cspr.idle`). 시트가 없는 다른 고양이(치즈·삼색·까망·하양)는 기존 SVG 매트릭스 2프레임 걷기를 그대로 사용(`catActorHTML`가 시트 유무로 분기, `CAT_SPRITES`). `prefers-reduced-motion`에서 걷기 정지. `sw.js` `v3.35.0`(APP_SHELL에 walk/idle PNG 추가).

### 변경 — 앱 이름을 "알뜰집"(코드/영문 Eggarden)으로 변경
- **앱 이름·로그인 화면 제목을 `알뜰집`으로 변경**: 브라우저 탭 제목(`<title>`), 설명 메타, iOS 홈화면 이름(`apple-mobile-web-app-title`), PWA 매니페스트(`name`·`short_name`·`description`), 로그인 화면 제목(`<h1>`), 더보기 하단 버전 표기(`가계부 v3`→`알뜰집 v3`)를 모두 **`알뜰집`** 으로 통일. **영문/코드 표기가 필요한 경우 `Eggarden`** 사용(예: `sw.js` 주석). 일반 명사 "가계부"(기능 설명·"내 가계부"·"개인 가계부" 등)는 유지. **영문/코드 표기가 필요한 경우 `Eggarden`** 사용. `sw.js` `v3.36.0`.
- **프로젝트 코드 식별자 `gglee` → `eggarden` 리네이밍**: 서비스워커 캐시 프리픽스 `CACHE_VERSION` `gglee-budget-*` → **`eggarden-v3.38.0`**(활성화 시 구 `gglee-budget-*` 캐시 자동 삭제). 문서의 프로젝트 경로 예시·아키텍처 캐시명·APK 패키지 예시(`com.gglee.budget`→`com.eggarden.app`)·README 파일트리 루트(`accountbook/`→`eggarden/`)도 정리. **Firebase 프로젝트 ID `money-bb658`는 실제 라이브 백엔드라 변경하지 않음**(변경 시 새 프로젝트 생성+데이터 마이그레이션 필요). 물리적 폴더명·GitHub 저장소명은 세션/외부에서 수동 변경 필요. `sw.js` `v3.38.0`.

### 추가 — 🛠️ 개발자 모드(펫알·박스 확률/구성 조작, canel94@gmail.com 전용)
- **개발자 전용 뽑기 튜닝 패널**: 특정 계정(`canel94@gmail.com`)으로 로그인한 경우에만 **설정 시트에 "개발자 모드" 토글**이 나타나고, 켜면 **"펫알 · 박스 설정"** 항목이 열려 뽑기 구성을 실시간으로 조작할 수 있음(`isDev`/`devOn`/`toggleDevMode`, `openDevGacha`). ① **등급 확률(%)** 을 등급별로 수정(합이 100이 아니어도 총합 기준 비율로 반영, `effTiers`/`rollTier` 정규화), ② **펫알=고양이별 등급**·**랜덤박스=가구별 등급** 재배정(`effCatTier`/`effItemTier` → `openGacha`에 반영), ③ **연출 테스트(무료)** — 등급 버튼으로 은화 소모/지급 없이 오픈 연출만 재생(`devPreview`, 특별+ 앞발 톡 육안 검증용). 설정은 **해당 기기(브라우저) localStorage**(`catDev`/`catDevCfg`)에만 저장 — RTDB·보안규칙 변경 없음, 다른 사용자·기기엔 영향 없음. 기본값 초기화 지원. `sw.js` `v3.33.0`.

### 변경 — 🥚 뽑기 오픈 연출 강화(등급색 빛 + 흔들림 + 앞발 톡)
- **아이템 등장 직전 연출 추가**(`fxClimax`): 마지막 균열/탭 이후 곧바로 열리지 않고 ① 알·박스가 **살짝 흔들리다가**(`fx-preshake`) ② **등급 색으로 빛이 새어나오고**(`.fx-leak`, 등급색 currentColor, 한정=레인보우 conic) ③ 버스트→등장. **특별 이상(특별·전설·한정)** 은 흔들림 도중 **은화 속 검은 도트 고양이 앞발(`M_PAW`)이 옆에서 들어와 톡 건드려** 한 번 더 크게 흔들린 뒤(`fx-hit`) 빛이 새어나오는 추가 연출. 박스는 빛이 샐 때 살짝 열림(`fx-ajar`). `prefers-reduced-motion`은 기존대로 즉시 결과. `sw.js` `v3.32.0`.

### 추가 — 🥚 고양이집 이벤트(뽑기) + 금화
- **펫알·랜덤박스 가챠 + 등급 확률 + 오픈 연출 + 금화**: 상점에 **이벤트 탭** 추가 — **펫알**(도트 흰 알+무지개 물음표, 열면 고양이)·**랜덤박스**(흰 상자+빨간 리본+은화 고양이얼굴, 열면 가구), 각 **100 은화**. ① **등급/확률**: 일반60·고급20·희귀15·특별3.8·전설1·한정0.2%(`TIERS`/`rollTier`/`rollFromPool`), 이름 텍스트 색(한정=레인보우). 테스트 배정 `CAT_TIER`(고등어~하양)·`ITEM_TIER`(가구). 한정 비면 전설로 대체. ② **구매+롤 원자적**(`openGacha`): 은화-100·**금화+1**, 신규 고양이/가구 지급, **중복 고양이는 30 은화 환급**. ③ **오픈 연출**(풀스크린 `#catFx`): 펫알=중앙 확대·부양→**3탭 균열**(`M_EGG`/`_C1`/`_C2`)→버스트→동물 등장, 랜덤박스=탭→**플래시·회전광선·파티클·컨페티** 화려 오픈→가구 등장. 등급 후광/색·`prefers-reduced-motion` 대응. ④ **금화**(`game.gold`, 은화와 동형 금색 `GOLD_PAL`) 코인바 표시. `sw.js` `v3.30.0`.

### 변경
- **리포트 6개월 추이 ↔ 선불·포인트 잔액 간격 확대**: 리포트에서 최근 6개월 추이(`.bars6`) 아래 **선불·포인트 잔액** 카드가 너무 붙어 보이던 문제 — 카드 상단 여백 `margin-top` 22→34px로 확대(`renderStats`). `sw.js` `v3.31.0`.
- **더보기 프로필 헤더 2행 정렬 정리**: 내 프로필/현재 가계부·그룹 두 행의 **아바타 크기를 44px로 통일**하고 동일 구조(`아바타 + 이름(flex:1) + 우측 액션`)로 재정렬 — **우측 액션(편집 chevron·전환 버튼)을 둘 다 오른쪽 끝 정렬**해 들쭉날쭉하던 우측 가장자리·이름 시작 위치를 맞춤. 멤버 아바타 겹침 나열은 이름과 전환 사이 우측에 정돈. `.prow`/`.grow` 공통 규칙화. `sw.js` `v3.29.0`.
- **🐱 고양이집 방 개선 — dock에 가구 배경 + 벽지 + 개별 랜덤 행동**: ① **dock가 웹캠 방으로**: 얇은 스트립 → 컴팩트 방(`.cd-room`, 벽지 배경 + **배치한 가구가 배경으로** + 걷는 고양이). 시트 방·dock 모두 가구는 배경(z-index), 고양이가 그 앞으로 지나감. ② **벽지 추가**(꾸미기 요소): `WALLPAPER_CATALOG`(기본·하늘·벚꽃·민트·별밤·살구), 상점 **벽지 탭**에서 구매·적용(`buyWall`/`applyWall`, `home/wallpaper`·`owned/wallpapers`), 방·dock 벽 배경에 반영. ③ **개별 랜덤 행동**: 고양이마다 속도·유휴빈도·방향전환·가구선호를 랜덤 부여, 걷다가 방향 바꾸거나 그 자리에서 앉기/식빵/낮잠 제스처(`buildActors` 성격 파라미터, `stepActors` turn/idle/seek + 쿨다운). `sw.js` `v3.28.0`.
- **더보기 상단 레이아웃 정리 + wsChip 그룹 프로필 사진 + 아이콘 확대**: ① **상단=내 프로필**(56px), 그 **아래 작게 현재 가계부/그룹**(36px) + 멤버 아바타 겹침 나열 + `전환`(탭 시 편집은 chevron 표시). ② **상단바 wsChip**(다크모드 토글 왼쪽 그룹 전환 칩)이 **현재 선택 그룹의 프로필 사진**을 표시 — 사진 없으면 점 대신 **이름 이니셜 아바타**로 폴백, 크기 16→20px(`updateWorkspaceChip`가 `wsAvatarHtml` 사용). ③ **기능 아이콘 확대**(`.gic` 50→58px, svg 24→28px)·**간격 축소**(`.grid4` gap→`4/2`, `.gcell` 여백 축소). ④ **전환 버튼** 우측 끝 강제 배치 해제(`.grow .gnm` `flex:0 1 auto`). ⑤ **로그아웃·앱 설치·버전**을 **화면 최하단으로**(`.more-wrap`/`.more-foot { margin-top:auto }`, 가운데). `sw.js` `v3.27.0`.
- **🐱 고양이집 놀이 고도화 — 포즈·가구별 상호작용·콘텐츠 추가**: ① **포즈 다양화**: 측면 앉기/식빵/낮잠 매트릭스(`M_CAT_SIT`/`M_CAT_LOAF`/`M_CAT_SLEEP`) + `catPose` 추가, 걷기 엔진에서 유휴·가구 근처 시 포즈 렌더. ② **가구별 상호작용**: 고양이가 배치 가구로 다가가 종류별 포즈 — 밥그릇=앉아먹기·방석=식빵·캣타워=낮잠·스크래처=앉기(`buildActors` props에 `itemId`, `poseForItem`). ③ **콘텐츠 추가**: 고양이 2종(**까망**·**하양**, 팔레트만) + 가구 1종(**스크래처**) + 상점/카탈로그. ④ 상점 구매 버튼 `aria-label`. *캣타워 2칸 차지·은화 원장은 보류.* `sw.js` `v3.26.0`.
- **더보기 상단 프로필 개편(내 프로필 + 현재 그룹·멤버) + 로그아웃 위치 이동**: ① 상단 큰 프로필 행(`.prow`)을 **가계부 프로필 → 내(사용자) 프로필**로 변경(아바타+이름, 탭 시 `openProfileSheet`). ② 그 아래 **현재 가계부/그룹을 작은 행(`.grow`)** 으로 표시(작은 아바타+이름+`그룹 · 멤버 N명`, 탭 시 `openWsProfileSheet`, 우측 `전환`). ③ 그룹일 때 그룹 행 오른쪽에 **멤버 아바타 겹침 나열**(`memberAvatarStack`, 최대 5 + `+N`). ④ **로그아웃**을 하단 리스트에서 빼내 **가운데 버튼**으로 `홈 화면에 앱 설치` 링크 위에 배치(기존 계정 리스트 `.lst` 제거). `sw.js` `v3.23.0`.
- **더보기 설정 정리(설정 시트 신설) + 코드 입력(은화 지급)**: ① **설정 시트**(`openSettingsSheet`) 신설 — 4열 그리드 고양이집 오른쪽에 **설정** 셀(`MORE_ICON.gear`) 추가, 기존 하단 리스트의 설정 항목(멤버·권한 관리·권한/공동 설정·거래내역·CSV 내보내기·다크 모드·고양이집 dock)을 이 시트로 이동. 더보기 본문 하단 리스트에는 **프로필·로그아웃**만 유지. ② **코드 입력** — 설정 시트에 코드 입력창 추가, `showmethemoney` 입력 시 **999 은화 지급**(`cats.js` `redeemCode`/`PROMO_CODES`, `submitPromoCode`). **사용자별 1회 제한** — 게임 노드 트랜잭션으로 사용 기록(`game/codes/{code}`)을 남겨 재입력 시 `이미 사용한 코드예요`(`normalizeGame`에 `codes` 보존 추가). ③ 그리드↔리스트 간격 확대(`.lst` margin-top 6→18px). `sw.js` `v3.23.0`.
- **더보기 앱 설치 안내를 최하단 작은 링크로 이동**: 더보기 상단의 `📲 홈 화면에 앱으로 설치하기` 큰 배너(`.install-banner`)를 제거하고, **화면 최하단(버전 표기 위)에 아이콘 없는 작은 텍스트 링크**(`.install-link`, `홈 화면에 앱 설치`)로 축소. `beforeinstallprompt` 사용 가능할 때만 노출(동작 `installApp()` 동일). `sw.js` `v3.23.0`.
- **고양이집 dock 스트립 간소화**: 하단 dock에서 **"고양이집" 텍스트 라벨(`.cd-lbl`)과 `>` 펼치기 버튼(`.cd-exp`) 제거** — 스트립 전체가 이미 탭 시 고양이집 시트를 여므로(은화 아이콘 포함) 중복 UI를 정리. 미사용 CSS도 함께 삭제. `sw.js` `v3.23.0`.
- **로그인 화면 로고를 고양이집 은화(코인) 도트 아이콘으로 교체**: 로그인 화면 로고(`.auth-logo`)를 앱 아이콘(`icons/icon.svg`) 대신 **고양이집 은화** 픽셀아트로 변경. `cats.js`의 `M_COIN`/`COIN_PAL` 픽셀맵과 동일한 아트를 정적 SVG(`public/icons/coin.svg`)로 생성해 참조(로그인 화면은 로그인 전이라 `cats.js` 런타임에 의존하지 않도록 정적 파일로 분리). 앱 셸에 `icons/coin.svg` 추가. `.auth-logo`의 `border-radius:20px` 제거(코인은 투명 배경이라 둥근 타일 클리핑 불필요). `sw.js` `v3.23.0`.
- **소비 대상과 출금 수단 분리 + 리포트 개인별/공동 지출 분리**: 지출·선불결제·포인트사용 거래 입력 시트에 **"소비 대상"** 선택(워크스페이스 멤버 또는 **`공동`**)을 추가해 **출금 수단(어느 계좌)** 과 **소비 대상(누구의 소비)** 을 분리. 집세처럼 공동 비용은 `공동`으로, 각자 쓴 용돈은 본인으로 귀속(`consumerField`/`recConsumerField` 추가, `openTxSheet`·`renderTxDyn`·`saveTx`·정기거래 `renderRecAccts`·`saveRecurring`에서 `tx.user`를 선택값으로 저장 — 기본값 본인). 리포트 "멤버별 지출"을 **개인별 지출(용돈)** 과 **공동 지출(집세 등)** 두 섹션으로 분리 표시(`renderStats`). 데이터 구조 변경 없음(`user` 필드 의미 확장). `sw.js` `v3.18.0`.

### 추가/변경 — 🐱 고양이집 (마무리)
- **고양이↔가구 상호작용 + 단일 rAF 걷기 엔진 + dock 간소화(코드 반영)**: ① **상호작용**: 방(고양이집 시트 홈)에 배치된 가구로 고양이가 가끔 다가가 잠시 머문 뒤 다시 배회(roam→goal→pause 상태머신). ② **단일 rAF 엔진**: 스트립·시트 방 두 걷기 루프를 하나로 통합 — "지금 보이는 무대"만 애니메이션(시트 열리면 방, 아니면 dock 스트립), 중복 rAF 제거. `document.hidden`·`prefers-reduced-motion` 존중. ③ **dock 간소화 코드 반영**: 인라인 확장(방 뷰)·펼치기 버튼(`cd-exp`)·라벨(`cd-lbl`)·미사용 CSS 제거 → 얇은 스트립+숨김만. `sw.js` `v3.24.0`.

### 추가 — 🐱 고양이집 (4단계)
- **최대 3마리 활성 관리 + 주간 미션 + dock 확장/숨김 + 더보기 코인 아이콘**: ① **활성 슬롯**: 홈 탭에서 보유 고양이를 탭해 **집에 내보내기/대기**(최대 3마리, `home/active`). ② **주간 미션**: 이번 주 5일 이상 기록(+20, 진행도)·리포트 확인(+10, `renderStats` 진입 시 `progress` 플래그), KST **월요일 초기화**·멱등 수령(미션 로직 일/주 period 일반화). ③ **전역 dock**: 얇은 스트립(탭 시 고양이집) + 더보기 설정 **고양이집 dock** 켬/숨김 토글. ④ **더보기 고양이집 아이콘 → 은화(코인)** 도트 아이콘. `sw.js` `v3.23.0`.

### 추가 — 🐱 고양이집 (2·3단계)
- **고양이 도트 아트 리파인 + 가구 상점 + 12×12 격자 배치**: ① **픽셀 아트**를 `design_sample`(측면 4다리·올린 꼬리·뾰족 귀·타비 줄무늬) 스타일로 재작성 — 정면 앉은 고양이(고등어/치즈/삼색 패치)·측면 걷기 2프레임(다리 교차), 좌표 드로잉으로 생성해 행 길이 검증. ② **가구 상점**(상점 탭 고양이/가구 서브토글): 방석·밥그릇·캣타워, **수량 허용** 구매(원자적). ③ **12×12 격자 배치 에디터**(배치 탭): 팔레트에서 가구 선택 → 빈 칸 탭 배치/회수, `home/placed`에 저장, **남은 수량** 표시. ④ **홈 방**에 배치 가구를 바닥에 매핑(가로=열, 앞뒤 깊이=행) 표시(걷는 고양이 뒤). `sw.js` `v3.22.0`. *다음: 최대 3마리 활성 심화·dock 인라인 확장·주간 미션.*

### 추가 — 🐱 고양이집 (은화 경제, 1단계)
- **은화 경제 코어 + 도트 아트 고양이 + 전역 dock + 고양이집 시트**(신규 `public/js/cats.js`): 가계부 활동으로 **은화(coin)** 를 모아 **고양이**를 입양하는 게임화 레이어. ① **경제**: `users/{uid}/game`(개인 전역, RTDB 규칙 변경 없음)에 `coins/owned/home/missions` 저장. **일일 미션**(오늘 거래 1건 +5 / 출석 +2) — KST 자정 롤오버, **멱등 수령**(게임 노드 트랜잭션 1회로 수령기록+지급 동시 처리 → 중복 지급 불가). ② **도트 아트**: 문자 매트릭스→SVG `rect`(crispEdges) 렌더(`pxSvg`), PNG 미사용(다크모드·캐시 자동 대응). 고양이 정면(고등어/치즈/삼색 코숏)·측면 걷기 2프레임·은화·가구 매트릭스. ③ **전역 dock**(`#catdock`, `#content` 형제 → 리렌더 무영향): 상시 얇은 스트립에 은화 배지 + **걸어다니는 고양이**(rAF, `visibilitychange`·`prefers-reduced-motion` 존중), 탭 시 고양이집 시트. ④ **고양이집 시트**(더보기 → 고양이집): **홈**(웹캠 방 뷰 + 걷는 고양이) / **상점**(고양이 3종 구매, 종당 1마리, 원자적 잔액 차감) / **미션**(일일 미션·수령). 카탈로그는 코드 상수(`CAT_CATALOG`/`ITEM_CATALOG`). *다음 단계: 가구 상점·12×12 격자 배치·최대 3마리 심화.* `sw.js` `v3.21.0`.

### UI 리디자인 (진행 중)
- **레이아웃 좌우 여백 대칭 수정**: `.content`(및 `.auth-wrap`)의 세로 스크롤바가 우측 여백을 잠식해 좌우가 달라 보이던 문제 수정 — 스크롤바 숨김(`scrollbar-width:none`+`::-webkit-scrollbar`)·`overflow-x:hidden` 추가로 좌우 20px 대칭 확보. `sw.js` `v3.20.1`.
- **가계부 프로필(이름·사진) 편집 + 달력 버튼 정리 + 리포트/여백 다듬기**: ① **가계부(워크스페이스) 프로필** — 사용자 프로필과 별개로 **가계부 이름+사진**을 변경(`openWsProfileSheet`/`saveWsProfile`, `workspaces/{wsId}/photo`에 256px JPEG 저장, 규칙 변경 없음—멤버 쓰기 허용). 더보기 프로필 행(`.prow`) 아바타=가계부 사진/이니셜로 표시하고 탭하면 편집(개인=항상, 그룹=소유자만 `편집` 배지). 상단 `wsChip`·가계부 전환 시트 행에도 사진 반영. ② **달력 "이 날짜에 추가" 버튼 제거**(FAB로 충분, `selectedDayHtml`). ③ **리포트** 선불·포인트 잔액 카드 위 여백 확대(`margin-top`). ④ **전체 좌우 여백 살짝 증가**(`.content`/`.topbar` 18→20px). `sw.js` `v3.20.0`.
- **카테고리 관리 개선(실시간 반영·아이콘/색 선택·터치 피드백) + Safari 키패드 줌 방지**: ① **시트 실시간 갱신** — 카테고리 순서변경(▲▼)·온오프 토글이 즉시 반영되도록 일반화된 훅(`state._sheetRefresh`) 도입: `openSheet`/`closeSheet`에서 해제, `rerender()`가 열린 시트의 본문만(스크롤 보존) 다시 그림. `renderCatManage`가 훅 등록. ② **아이콘 라인 그리드 선택** — 카테고리에 `iconKey` 필드 추가, `catSvgIcon`이 저장된 키 우선 사용(이름 매핑→tag 폴백). 편집 시트의 이모지 입력을 **라인 아이콘 그리드**(`.icon-grid`)로 교체(`pickCatIcon`), 전체 카테고리 아이콘 변경 가능. `CAT_SVG`에 가계부용 라인 아이콘 **19종 추가**(paw·car·fuel·scissors·dumbbell·cart·shirt·baby·gamepad·music·ball·bolt·droplet·piggy·trend·doc·donate·ticket·monitor, 총 46종). ③ **색상 팔레트** — `CAT_PALETTE`를 중복 없는 차분한 톤(`CAT_FALLBACK`)으로 교체, 스와치 그리드(`.swatch-grid`) UI. ④ **터치 피드백** — `.acct`·`.menu-item`·`.lrow`·`.chip`·`.gcell`에 `:active` 추가. ⑤ **Safari 더블탭 줌 방지** — `button`/`.kp button`에 `touch-action:manipulation`(+키패드 `user-select:none`), 핀치 줌은 유지. ⑥ 예산·구독·정기 카테고리 select 드롭다운의 이모지 접두 제거(이름만). `sw.js` `v3.19.0`.
- **리포트 도넛 버그 수정 + 카테고리 색 중복 제거 + 기능시트 라인아이콘화 + 상단바/전환시트 디테일**: ① **리포트 카테고리 도넛 미표시 버그 수정** — `catColor`가 색 없는/미등록 카테고리(예: `엔터`)에서 무효값을 반환해 `conic-gradient` 전체가 깨지던 문제. `catColor`에 hex 검증 + 이름 해시 폴백(`CAT_FALLBACK`)을 추가해 **항상 유효한 색 보장**. ② **카테고리 색 중복 제거** — `CAT_META`를 서로 겹치지 않는 25색(구버전 `엔터·식사·생활·교육` 포함, `경조사≠경조사비수령` 등) 차분한 팔레트로 재구성, `엔터` 색 부여. `constants.js` 기본 색도 동기화. ③ **기능 시트 라인아이콘화** — 경조사비·예정·인맥·대출 상환·공개범위 항목의 솔리드 타일+이모지 → **중립/라인 SVG**(`giftSvgIcon`/`GIFT_SVG_KEY`, `user`/`coin`/`heart`/`flower` 아이콘), 예산·정기결제 행에 **작은 tint 라인아이콘 타일**(`catTileMini`/`.mtile`, `budgetTile`). ④ **상단바·전환시트** — `wsChip` 이모지 → `.dotk` 점+이름, 가계부 전환 시트 행을 라인아이콘 타일(개인=home·그룹=people)+**체크 표시**로. `sw.js` `v3.17.0`.
- **로그인 화면 시안화 + 로고/다크토글 아이콘 교체**: ① **로그인 화면**을 핸드오프 톤으로(로고 64px·타이틀 26/700·`--bd` 인풋·검정 CTA + **초대코드 안내 푸터** `.authfoot`). ② **앱 로고 교체** — 파랑 `₩` → **흑백 고양이 은화(silver coin) 단순 아이콘**(`public/icons/icon.svg` 새로 작성: 실버 라디얼 코인 + 검정 고양이 얼굴, 흰자/은색 수염). `manifest.webmanifest`에 확장형 SVG 아이콘 추가·테마색 `#ffffff`. ③ **상단 다크모드 아이콘** 이모지(🌙/☀️) → **시안 선형 SVG 해/달**(`core.js` `ICON_SUN`/`ICON_MOON`, `currentColor` 흑백). `.icon-btn`(36px)에 svg 규격 추가. `theme-color` 흰색으로. *PWA 설치용 PNG(192/512)는 변환 도구 부재로 미갱신 — 동봉 `regen-icons.html`로 재생성 후 교체 필요.* `sw.js` `v3.16.0`.
- **디자인 핸드오프 v2 전면 개편 — 토큰 팔레트 + 카테고리 옅은 색/라인 아이콘 시스템**: 새 핸드오프(`design_handoff_household_ledger/`)에 맞춰 ① **토큰 팔레트 교체**(`styles.css` 라이트/다크 — `--text #191F28`·`--sub #8B95A1`·`--expense #F04452`·`--income #3182F6` 등 + **카드/인풋 테두리 토큰 `--bd #E5E8EB` 신규**), ② **카드 스타일 변경**(`.card` soft 채움 → **흰 배경 + 1px `--bd` 테두리**, radius 20), ③ **카테고리 색/아이콘 시스템**(`core.js`): 기본 카테고리 22종을 핸드오프 팔레트로 오버라이드(`CAT_META`)하고 **얇은 라인 SVG 아이콘 라이브러리**(`CAT_SVG`) 도입 — 거래행·카테고리 타일이 **13% 알파 tint 배경 + 솔리드 색 라인 아이콘**으로(헬퍼 `catColor`/`catSvgIcon`/`catTileStyle`/`hexA`/`svgWrap`, **이모지→라인 아이콘**). 거래행 타일 38px 솔리드+이모지 → **46px tint+라인 SVG**(`txRowHtml`/`TX_SVG_KEY`), 카테고리 관리행도 동일. ④ **자산 순자산 카드 검정 hero → 흰 카드+테두리**(`.assethero`, 카드대금 빨강), 계좌/은행 아이콘은 **중립 회색 타일**(`acctRowHtml`). ⑤ 리포트 도넛 기타색·인풋/섹션헤더 여백 정리. 기본 카테고리 색(`constants.js`)도 핸드오프 톤으로. 기능 상세 시트는 토큰·카드 테두리 자동 상속. 헤드리스 문법 검증(에러 0). `sw.js` `v3.15.0`.
- **자산 화면 시안 1:1 재현**(`renderAssets`): 상단 **순자산 다크 hero**(`.assethero` — 순자산 큰 숫자 + 하단 분리행 `총자산`/`카드대금`/`계좌 N개` pill, `순자산=총자산−카드대금` 실데이터 계산) → 섹션을 **`.sech` + 원형 `.addbtn`**(＋ SVG)로 통일(`sechHtml` 헬퍼·`PLUS_SVG`) → **입출금·현금 / 선불·포인트 / 기타** 계좌 행을 시안 `.acct`로(유형별 **라인 SVG 아이콘** `acctIcon`: 은행·현금·카드·포인트·간편결제 등, 색은 `a.color` 유지, sub=`유형 · 소유자`) → **카드 실적**을 `.perfrow`(`.perftop`/`.pct`/`.prog`/`.perfsub`, 헤더 우측 현재 월, 달성=인컴색) → **적금 목표**를 예산행 톤 `.bgrow`(`.bgtop`/`.bgtrack`/`.bgfill`, 색 팔레트+이니셜 칩). 카드 실적·적금 추가/수정 동작(`openAcctSheet`/`openSavingsSheet`) 유지. 헤드리스 문법 검증(에러 0). `sw.js` `v3.14.0`.
- **리포트 화면 시안 1:1 재현**(`renderStats`): **월 네비**(`.monthlbl` ‹ › — `state.month` 연동, 달력과 동기화) → **총지출 카드**(`.bigexp` 큰 숫자 + 수입/잔액/전월 대비 %, 증가=레드·감소=블루) → **카테고리별 CSS 도넛**(`conic-gradient` + `.legend`, 상위 5 + 기타, 카테고리 색) → **최근 6개월 추이 막대**(`.bars6`, 표시 월 잉크 강조) → **멤버별 지출 바**(`.mbar` 색+금액+비율, `wsMemberNames` 연동). 우리 고유 카드(예산·목적별 진행률·선불/포인트 잔액)는 시안 섹션 **아래에 유지**. **Chart.js 의존 제거**(`drawCharts`·canvas·CDN script 삭제) → 순수 CSS 차트. `shortAmt`/`signComma` 헬퍼 추가. 헤드리스 검증(에러 0). `sw.js` `v3.13.0`.
- **탭바 SVG 아이콘 + 더보기 화면 시안 1:1 재현**: 하단 탭바(달력·리포트·＋·자산·더보기)·FAB의 이모지를 **시안 SVG 라인 아이콘**으로 교체(`index.html`). 더보기 화면(`renderMore`)을 통째로 재구성 — **프로필 행**(`.prow` 52px 그라데이션 아바타+가계부명+`그룹·멤버 N명·초대코드`/`개인 가계부`+잉크 [전환] 칩), **4열 기능 그리드**(`.grid4` soft 스퀘어클 SVG: 예산·구독[활성 구독 수 뱃지]·정기결제·목적별·정산·경조사비·대출/이자·카테고리), **설정 리스트**(`.lst`/`.lrow` SVG아이콘+값+chevron: 멤버·권한 관리[그룹 전용·N명]·권한·공동 설정·거래내역·CSV 내보내기·다크 모드[현재 상태]·프로필[아바타]·로그아웃). 실데이터 연동(`state.wsMeta` 이름·멤버·초대코드, `state.subscriptions` 활성 수, 테마). 개인 가계부는 멤버 행 숨김. 헤드리스 검증(에러 0). `sw.js` `v3.12.0`.
- **거래 입력 시트 시안 1:1 재현**(`openTxSheet`): 의미색 **유형 세그**(지출=레드/수입=블루/이체) → **큰 금액 표시**(`.amtbig` ₩+읽기전용) → **카테고리 칩 가로 한 줄**(`#sCatChips`, 실제 RTDB 카테고리·이름+카테고리 색 점, 선택 시 잉크) → **필드 행**(`.txfield` 출금/결제 수단=실제 계좌, 날짜=date, 설명) → **숫자 키패드**(`.kp` 1~9·00·0·⌫, `kpPress`/`kpDel`로 `#sAmount` 포맷 입력). 시안에 없는 우리 고유 기능은 키패드 아래 **상세 설정 접기**(`#sAdv`)로 보존 — 목적별 가계부 연결·정산 블록·카드 실적·메모, 선불/포인트·이체 유형(이체는 출금/입금 2행), 실소비 포함. 카드 결제수단 선택 시 상세 자동 펼침. 저장 경로(`saveTx`) 무변경. `sw.js` `v3.11.0`.
- **달력 화면 시안 1:1 재현**: 멤버 칩 가로 스크롤(그룹, 컬러 그라데이션 아바타 `avatarGrad`)+멤버 필터(`state.memberFilter`), 상단 요약을 **연회색 msum 박스**(수입/실제소비/합계)+보조행(충전·선불·미사용)·예산 미니바 유지, **월요일 시작** 달력(`calendarGridHtml`)·콤팩트 셀·잉크 오늘 원형·주말 색, 셀 **카테고리 색 점**(텍스트 금액 제거), **선택일 인라인 거래**(`selectedDayHtml` `.sech`+squircle 거래행, 탭=`selectDay`), 거래행 sub를 `카테고리·기록자`로 간결화. `sw.js` `v3.10.0`.
- **0단계 — 디자인 토큰·공용 컴포넌트**: 새 시안(잉크 모노톤)에 맞춰 `styles.css` 팔레트(라이트/다크)·타이포·radius·그림자와 공용 컴포넌트(`.card`·`.btn`·`.input`·`.chip`·`.seg`·`.tab/.tabbar`·`.fab`·`#sheet`·`.tx`·`.menu-item`·요약·progress·`.toast` 등)를 리스타일. 주색=잉크(블랙↔다크 반전), 수입=블루·지출=레드, 카드=soft 회색 박스, FAB=squircle. JS·구조 변경 없이 전 화면 리스킨. `sw.js` `CACHE_VERSION` `v3.8.0`. (다음: 크롬 SVG 아이콘 → 화면별 디테일)

### 추가
- **프로필 사진 / 이름 + 그룹 멤버 프로필**: 더보기 프로필 줄(아바타·이름) 탭 → 내 프로필 시트(`openProfileSheet`)에서 **기기 사진으로 프로필 설정**(브라우저 캔버스로 256px JPEG 리사이즈 → base64로 `users/{uid}/photo` 저장)·삭제 + **별명(이름) 수정**(모든 워크스페이스 멤버 이름 비정규화 갱신). **그룹 관리 멤버 목록에 멤버 아바타** 표시(`avatarHtml`, 사진 없으면 이니셜 폴백). 멤버 사진은 `loadMemberPhotos`로 캐시. Firebase Storage·보안규칙 변경 없음. `sw.js` `CACHE_VERSION` `v3.7.0`.

### 수정
- **모바일 화면 드래그/바운스 수정**: 문서(body) 스크롤 구조 → **앱셸 고정 레이아웃**으로 전환. 바깥 페이지를 뷰포트에 딱 맞게 잠그고(`body { overflow:hidden; overscroll-behavior:none }`, `#app` 세로 flex `100dvh`), **내용 영역(`.content`)만 내부 스크롤**(`overflow-y:auto; overscroll-behavior:contain`). 상단바·하단탭바는 flex 고정 행, 시트/로그인도 `100dvh` 기준. iOS 고무줄 오버스크롤·`100vh` 점프 해소. `sw.js` `CACHE_VERSION` `v3.6.0`.

### 접근성/UI
- **접근성(A11y) 다듬기**: 화면 확대 허용(viewport `maximum-scale` 제거), 시트를 `role="dialog" aria-modal`로·Esc 닫기·포커스 이동/복원·포커스 트랩, 토스트 `aria-live`, 아이콘 버튼 `aria-label`(닫기·거래추가·테마·가계부전환), `<main>`/`<nav>`+탭 `aria-current`. `innerHTML` 재렌더에 대응하는 **중앙 A11y 레이어**(MutationObserver+델리게이션): 커스텀 토글에 `role="switch"`·`aria-checked`·키보드 활성화, `onclick` 달린 `div`에 `role="button"`·`tabindex`·Enter/Space 활성화, `.field` 라벨↔입력 연결. CSS: `:focus-visible` 링, 탭 타깃 ≥44px, `prefers-reduced-motion` 대응, `.sr-only`. `sw.js` `CACHE_VERSION` `v3.5.0`.

### 추가
- **권한 / 공동 설정**: 더보기 → 🔒 권한/공동 설정(`openSharedSettings`). ① **멤버/권한 관리**(소유자 전용) — 그룹 이름 변경·소유자 이전·멤버 내보내기를 `openGroupManageSheet`에 추가(휴면 상태였던 `role` 활성화, 앱 UI에서 권한 게이팅). ② **공동 기본값** — 새 항목 기본 공개범위·기본 소유자를 `ws/{wsId}/settings`에 저장하고 생성 폼 기본값에 반영(`defaultVisibility`/`defaultOwnerName`). ③ **공개범위 개요** — 전 엔티티의 `private` 항목을 모아 보고 개별/일괄 공개 전환. 보안규칙 변경 없음(UI 제한). `sw.js` `CACHE_VERSION` `v3.4.0`.
- **대출 / 이자 관리**: 더보기 → 🏧 대출/이자(`openLoanBook`). 빌림(내 빚)·빌려줌 구분, 원금·연이율·기간·기본 상환계좌. 대출별 상세(`openLoanDetail`)에서 **잔액·누적 이자·상환률**, 잔액 기준 **월 예상 이자(단리) 산출**, 상태(상환중/완료/연체). **상환 기록**(`openLoanPayment`)에 원금+이자 입력 → 잔액 자동 차감. 옵션 "이자를 가계부 거래로 기록"(기본 ON) → 빌림=`대출이자` 지출(실제소비)/빌려줌=`이자` 수입 생성·연결(`loanId`, 거래 행 🏦 배지). **원금 이동은 장부로만 관리**(가짜 계정·이중집계 방지). 신규 노드 `loans`·`loanPayments` + 보안규칙. `sw.js` `CACHE_VERSION` `v3.3.0`.
- **경조사비 관리**: 더보기 → 경조사비. 보냄/받음/순 요약과 **[기록][예정][인맥] 탭**. 경조사비 기록(상대·관계·경조사 유형·줌/받음·금액·날짜) 추가/수정/삭제, 상대는 **인맥 자동 등록·매칭**. 옵션 "가계부 거래로도 기록"(기본 ON) → 줌=`경조사` 지출/받음=`경조사비 수령` 수입 거래를 생성·연결(`giftEventId`, 거래 행에 🎁 배지). 경조사 **예정** 등록·완료(→기록 프리필), **인맥**별 보냄/받음 합계. 기존 예약 노드 `people`/`giftEvents`/`plannedGiftEvents` 활용(보안규칙 기존 그대로). `sw.js` `CACHE_VERSION` `v3.2.0`.
- **공동 지출 정산(Step 9)**: 목적별 가계부에서 공동 지출을 정산. 거래 입력 시 정산 포함·결제자·분담 방식(균등/직접/결제자부담)·참여자·부담 금액 지정(선택 PB의 `settlementEnabled`일 때만 노출). 목적별 상세에 **[거래][정산] 탭** 추가 — 참여자별 결제/부담/잔액, 단순 송금 제안, 완료·취소 처리, 요약(총 공동지출·대상 건수·미정산/완료 금액·완료율). 목적별 카드·더보기 메뉴(`정산`)에 상태 표시. 신규 RTDB 노드 `settlementPayments`(per-uid)와 보안규칙 추가. 정산 송금은 기본 기록만(실제소비/예산/리포트 중복 없음), 옵션으로 `transfer` 거래 동시 생성. `sw.js` `CACHE_VERSION` `v3.1.0`.

### 변경
- **프로젝트 구조 정리**: 웹 앱을 `public/` 폴더로 이동(`css/`·`icons/`·`js/` 분류), 배포 가이드를 `docs/deploy/` 로 이동(소문자 파일명). `netlify.toml` `publish="public"`. 설정 파일(`firebase.json` 등)은 저장소 루트 유지. 기능 변경 없음 — 경로/문서 링크만 정리.

## [3.0.0] - 2026-06-29

가계부 v3 — 워크스페이스(개인/그룹) 모델 + PWA/APK 배포 기반 구축.

### 추가
- **개인/그룹 워크스페이스 모델**: 모든 가계부 데이터를 `ws/{wsId}` 아래로 격리. 그룹은 6자리 초대코드로 합류, 멤버 간 공동 가계부 공유.
- **PWA 지원**: 웹 매니페스트, 서비스워커(앱 셸 캐시 v3.0.0), 홈 화면 설치.
- **iOS PWA 지원**: apple 홈화면 앱 메타 태그 추가(전체화면 앱처럼 실행).
- **APK/TWA 패키징 준비**: PWABuilder 기준 아이콘(192/512 + maskable), `.well-known/assetlinks.json` 템플릿, [APK.md](deploy/apk.md) 안내.
- **배포 설정**: Netlify(`netlify.toml`), Firebase RTDB 규칙(`database.rules.json`, `firebase.json`, `.firebaserc`), 관련 가이드 문서.
- 거래 9종 타입, 카드 실적, 예산, 정기결제, 구독, 목적별 가계부, 적금 목표, 다크모드, CSV 내보내기.

### 변경
- **PWABuilder 대응**: 매니페스트에서 SVG 아이콘 제거(PNG만 사용), `manifest.webmanifest` content-type 지정.

### 마이그레이션
- 구버전(v2) 전역 단일 트리 데이터를 로그인 시 "공유 가계부" 그룹 워크스페이스로 1회 자동 이전(`migrateLegacyIfNeeded`, `migrationV3` 플래그). 원본은 백업 보존.

---

<sub>이 시점 이전(v3.0.0으로 묶기 전)의 세부 git 이력: `62a3237`, `5e8157e`, `1263145`.</sub>
